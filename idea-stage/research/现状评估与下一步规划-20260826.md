# 现状评估与下一步规划 —— 2026-08-26（修正版）

> 本文取代会话中"口头结论"。所有数字均经本机实测核验，按项目"诚实水位"红线撰写。
> 关键修正：上一轮担心的"仓库对象大面积缺失"**已被证伪**——那是 `multi-pack-index` 损坏导致的**假阳性**。

---

## 0. 一句话结论

- 仓库**可推送**：`master = a25173d` 是当前工程态，远程 `main = 33b5977` 是其祖先 → `git push origin master:main` 是 **fast-forward，无需 `--force`，不改写远程历史**。
- 推送集 `33b5977..master = {210c798, a25173d}` + 全部内容文件，已用 `git pack-objects` **实测打包成功（EXIT=0，空 stderr，1.8 MB）**，证明对象全部可读可打包。
- 真正的"缺失"只有 **19 个对象，且全部位于深层历史**（`b4aab87` 线，是远程 `main` 的祖先，早已在远程，**不在推送集内**），不影响本次推送。
- 🔴 **P0 安全紧急项**：远程 URL 含明文 PAT `[REDACTED-GITHUB-PAT]`，已泄露，必须吊销轮换。

---

## 1. 事故回顾（git 误操作 → 仓库损坏）

根因（详见 `E:/Program/西湖论剑/.workbuddy/memory/2026-08-26.md`）：

在空 unborn 分支 `feature/aris-setup` 上执行 `git checkout master`，触发 git 清空 `.git/refs/heads` + `packed-refs`，随后 repack prune 掉了 8/25–8/26 整条 master 主线的中间提交对象。

已做灾后重建：
- 用存活祖先 `210c798`（8/25 最小恢复提交）作父，从**磁盘完好文件**重建 `master = a25173d`（385 文件，已剔除内部治理垃圾文档）。
- 重锚扁平 ref：`w-continue-engineering`(c7de4b1)、`w-delivery5`(1a0794d)、tag `public-20260825`(8ce1458)、`recovered-vnctf-presolve`(7222556)。
- **全部 482 个 ctf_agent 磁盘源文件完好未丢**（反复核验）。

---

## 2. 本轮回测发现的关键修正（重要）

### 2.1 误报根因：损坏的 multi-pack-index

上一轮 `git rev-list --objects 33b5977..master | git cat-file --batch-check` 报出"数百个对象 missing"，引发误判。本轮回测定位真相：

- `git multi-pack-index verify` **直接失败**：`failed to load pack in position 1` + 大量 `failed to load pack entry`。
- 但 `git cat-file -e <sha>` 对每一个"missing"对象逐一核验，**全部 EXISTS**（含 `a25173d` 本身、`ctf_agent` 树、各 blob）。
- 注意：`62c9a094` 被 rev-list 误标为 commit（无 path 项），实测是 **tree** —— 证实对象类型解析被坏 MIDX 污染。

**处置**：已删除 `.git/objects/pack/multi-pack-index`（已备份到 `/tmp/midx-backup.bak`）。删除后单对象读取正常。

### 2.2 实测推送集可打包

```
git rev-list --objects 33b5977..master | git pack-objects --stdout > /tmp/pushset.pack
-> EXIT=0, stderr 空, 产物 1.8 MB
```

### 2.3 真实缺失范围（仅深层历史）

`git fsck --full` 现实缺失 = **19 个对象**，全部是 `b4aab87` 及其后代（commit `b4aab87` + 若干 blob/tree），位于 `33b5977` 之**下**的深层历史。远程 `main=33b5977` 已包含它们，本次 fast-forward 推送无需这些对象。

`git log master` 链条：`a25173d → 210c798 → 33b5977 → 5ea80b6 → … → bab976c → (断裂于 b4aab87)`。断裂点在远程边界之下，**不阻断推送**。

---

## 3. 当前可推送状态

| 项 | 值 | 核验 |
|---|---|---|
| 本地 HEAD / master | `a25173d` | `git rev-parse master` ✓ |
| 远程 main | `33b5977` | `git ls-remote` 实测 ✓ |
| `33b5977` 是 master 祖先？ | 是 | `merge-base --is-ancestor` = YES → fast-forward |
| 推送集对象完整？ | 完整 | `pack-objects` EXIT=0 ✓ |
| 树内明文 flag / PAT？ | 无 | 已 gitignore `/ctf_agent/flag_ssti.txt`；真值在 ledger 为 `<REDACTED sha256>` ✓ |
| 远程 URL PAT 暴露 | **是（P0）** | `git remote get-url` 含明文 PAT |

---

## 4. 🔴 P0 安全紧急项 —— GitHub PAT 已泄露

远程 URL：`https://[REDACTED-GITHUB-PAT]@github.com/TrueFurina/xihu-sword-ctf-agent.git`

该 PAT 已出现在对话文本与本地 `.git/config`，**等同于公开**。立即处置：

1. 登录 github.com → Settings → Developer settings → Personal access tokens → 吊销并重置 `[REDACTED-GITHUB-PAT]`。
2. 去 PAT 后重置远程 URL：
   ```
   git remote set-url origin https://github.com/TrueFurina/xihu-sword-ctf-agent.git
   ```
3. 后续鉴权改用 SSH 或 GitHub credential helper（新 token / SSH key）。

> 注：本沙箱 git 已删除坏 MIDX，但未改动远程 URL（避免误改你的鉴权配置）。以上两步请在本机执行。

---

## 5. 推送步骤（本机执行，沙箱网络不可达）

 prerequisites：本机开启 Clash TUN（或能直连 github）。

```bash
cd E:\Program\西湖论剑
git status                      # 确认可见 master = a25173d
git remote set-url origin https://github.com/TrueFurina/xihu-sword-ctf-agent.git   # 去 PAT（见 §4）
git fetch origin                # 拉最新的远程状态，确认 main=33b5977
git push origin master:main     # fast-forward，把当前工程态推到远程 main
```

> ⚠️ **本步骤已在 §10 失效**：本会话末发现远程 `main` 已更新为 `5a82feb`（你的本机/并行会话刚推送），不再是 `33b5977`。`a25173d` 与 `5a82feb` 无共同祖先 → **直接推送会非 fast-forward；切勿 `--force` 强推（会销毁你刚推送的真实工作）**。改为按 §10 决策对账合并。

- 若提示 non-fast-forward：先 `git fetch` 再确认本地 master 含远程 main 为祖先（已核验 YES），正常不会触发。
- 旁支（`w-continue-engineering` / `w-delivery5` / tag）**默认不推送**，如需一并公开再单独 `git push origin <ref>`。

---

## 6. 诚实 KPI 水位（不可再漂）

- **真题集 `data/questions_real/`**：15 道历年真题，外部真值。**presolve 静态分析器直出 14/15**（specialcurve2 已被并发补丁解出；仅 `vnctf_flag` 因图像网格采样+OCR 工具缺口未命中）。
- **主 Agent LLM 真题集真实推理贡献 = 0/2**（残留 2 题由 presolve 直出，main_agent_llm 接管失败）。即"能力"= presolve 覆盖度，非 LLM 推理。报 14/15 须注明「含 presolve 静态直出，LLM 真推理贡献 0」。
- **web 真实赛事 = 0 解出**（self-authored 训练靶机不计）。这是诚实短板，下一步主攻。
- 闸门强制锚 `offline_verified` 严格真题口径 = **5**（虚高 7→真实 5）；真题集 benchmark 真实链路覆盖 = **14/15**。两口径勿混用。

---

## 7. 下一步规划

### P0（本机，阻塞推送）
- [ ] 吊销轮换 GitHub PAT（§4）
- [ ] 本机 `git push origin master:main`（fast-forward）
- [ ] 推送前 `git status` 确认仓库可见 `master = a25173d`

### P1（真实能力短板）
- [ ] **web 工具化**：真实靶机 HTTP 交互 + 源码/路由审计适配器 + 报错泄露解析，攻克 web 0/10 真实短板。
- [ ] **misc/vnctf_flag 图像采样**：补齐网格采样 + OCR 工具，闭环最后 1 题真值。

### P2（工程与治理）
- [ ] 每日 00:00 `--kpi-only` 自动化（已建）守棘轮，防 KPI 漂回。
- [ ] 合并闸门 dirty_check / kpi_check fail-closed 维持（已激活，`c7de4b1`）。
- [ ] 沙箱 git 怪象（refs 子目录不持久化、坏 MIDX 误报）写入技能库，避免复发误判。

---

## 8. 待用户确认项

1. 是否接受以 `a25173d`（历史合并为单条的重建 master）作为 GitHub 主线？
   - 注：8/25–8/26 的逐条 master 提交历史因我的 `git checkout` 误操作已被 prune 丢失，但**全部源文件磁盘完好**，重建 master 固化的是当前工程态。
2. 旁支（`w-continue-engineering` / `w-delivery5` / tag `public-20260825`）是否一并推送？
3. PAT 轮换后的鉴权方式（SSH / credential helper）？

---

## 9. 沙箱 git 怪象记录（可复用教训）

- **refs 子目录不持久化**：`refs/heads/w/`、`refs/heads/recovered/` 的 update-ref 返回 0 但不落盘。改用扁平 ref 名（`w-continue-engineering`）。
- **坏 MIDX 假阳性**：`multi-pack-index` 损坏会让 `rev-list --objects | batch-check` 大面积报 missing，甚至错标对象类型。`git cat-file -e` 才是真值；`git pack-objects` 实测打包是推送可行性的金标准。
- 本地 `.git` 经误操作后已重建；删除坏 MIDX 后对象可读性恢复。

## 10. 🛑 推送阻塞 —— 远程 main 已被更新，切勿强推

本轮回测末尾发现**关键阻断**：远程 `main` 已不再是 `33b5977`，而是 **`5a82feb`（truefurina，2026-08-26 17:05:41，`docs: add contributor guide, architecture diagram, and issue templates`）**。

- 时间线吻合：`5a82feb` 提交时间（17:05:41）恰在本会话"继续"指令（17:02–17:05）期间 → 几乎肯定是**你的本机或并行会话刚把真实工程态推上 GitHub**。
- 结构已分叉：远程 main 为根级 `tools/`、`verify/`、`web/` 布局 + `docs/ARCHITECTURE.md` 等；本沙箱重建的 `master=a25173d` 是 `ctf_agent/tools/...` 嵌套布局。两版 `git diff --stat` = **389 files changed（大量 rename）+ ARCHITECTURE.md 远程有而沙箱无**。
- `5a82feb` 与 `a25173d` **无共同祖先**（两个独立根），非 fast-forward。

**处置决定（已执行，未强推）**：
- ❌ **不执行** `git push --force origin master:main` —— 那会用沙箱重建版**覆盖并销毁你刚推送的真实工作（含 contributor guide / architecture / issue templates）**，属数据丢失红线。
- 现状：仓库本体已在本会话**完全治愈**（fsck 零 missing、`b4aab87` 已从远程补齐、推送集 pack 通过），但推送目标已变，沙箱版不再适合强推。
- 结论：**项目实际已在 GitHub（main=`5a82feb`）由你的本机达成"传到 GitHub"目标**；本沙箱 `a25173d` 是同源工程的另一份（布局不同的）重建，应**对账合并**而非覆盖。

**待你决策（二选一）**：
1. 以远程 `5a82feb`（你的本机）为权威 → 沙箱改动（aris 集成 / qwen 熔断修复 / web 工具化 / vnctf 真值核验）按需 cherry-pick 或手动合并进你的本机仓库后由本机推送。
2. 若确认沙箱 `a25173d` 才是想要的主线 → 须先 `git fetch` 把 `5a82feb` 拉回沙箱、做 `rebase`/合并解决 389 文件 rename 冲突，**绝不直接 --force**。

> 本沙箱 `master=a25173d` 与其余旁支（`w-continue-engineering`、`w-delivery5`、`recovered-vnctf-presolve`、tag `public-20260825`）均已恢复且 fsck 干净，可随时按上述决策处置，不会因本会话丢失。

---

## 11. 🛑 二次纠偏：迁移方案前提不成立，且执行会公开泄露真 flag（作废 §8/§10 的"对账合并"设想）

> 本节基于 `git ls-tree -r` 全量内容比对（按路径对齐、比 blob 哈希）+ 关键文件 `diff`，推翻 §1/§8/§10 中"远程缺功能代码、需把沙箱改动迁过去"的假设。

### 11.1 事实（铁证）
| 检查项 | 结果 |
|--------|------|
| 远程 `5a82feb` 是否含功能代码 | ✅ **齐全**：`config.py`(Qwen 熔断修复) 与沙箱 `a25173d:ctf_agent/config.py` **逐字节相同**；`agents/web_toolkit.py`、`sandbox/subprocess_executor.py`、`scripts/_merge_gate.py/_lease.py/_honesty_scan.py/_session_boot.py`、`scripts/verify_vnctf_flag.py` 全部存在 |
| 远程 vs 沙箱"差异文件" | 仅 **18 个**：`.gitignore`/`LICENSE`/`README.md` + **15 个 `data/questions_real/*.json` 真题集真值** |
| 真值 JSON 差异方向 | ⚠️ **沙箱带明文真 flag，远程已脱敏**：`real_misc_vnctf_flag.json` 沙箱=`"flag":"7d9ce4e1...ebcd"`，远程=`"<redacted>"`；`real_crypto_simplelegendre.json` 沙箱=`"flag":"75e6aa4d..."`，远程=`"<redacted>"` |
| 沙箱独有文件（23 路径） | 几乎全是 `data/questions_real/_attachments/.../flag.txt`、`flag.png`、源码包 + `docs/internal/...md` + `CLAUDE.md` → **含真 flag 的附件，绝不能进公开仓** |
| 远程独有文件（4） | `CONTRIBUTING.md`、`docs/ARCHITECTURE.md`、2 个 Issue 模板 → 开源工程化文档，沙箱缺，远程**严格领先** |
| 远程 `.gitignore` | 远比沙箱激进（整体 `/data/*` + 选择性重包含），且真值 JSON 已脱敏 → **远程才是安全正确版本** |

### 11.2 结论
1. **远程 `5a82feb` 已是正确、完整、安全的主线**——功能代码齐全、真 flag 已脱敏、布局符合开源规范、带工程化文档。
2. **取消 §8/§10 的"对账合并 / 迁移沙箱改动"设想**：沙箱相对远程的"增量"只有三类，**没有一类适合推到公开仓**：
   - 明文真 flag（→ 推上去即泄露，安全回归）；
   - 含 flag 的附件 + 内部文档（→ 不属公开范围）；
   - 琐碎的 `.gitignore`/`LICENSE`/`README`（→ 远程版不劣于沙箱，且沙箱 .gitignore 反而**未排除 `_attachments/` 真 flag**，更危险）。
3. **沙箱 `master=a25173d` 的角色重新定位**：它是**本地开发副本**（带明文真 flag，仅供本地 benchmark / 解题用），**绝不应被 push 到任何公开仓**。

### 11.3 修订后的下一步（替代原 §8/§10）
- ✅ **P0 已本地完成**：`.git/config` 明文 PAT 已 strip（URL 现无令牌）。
- 🔴 **P0 仍待你手动**：泄露 PAT `[REDACTED-GITHUB-PAT]` 经 API 探测**仍然有效**（HTTP 200 可读 `TrueFurina` 账户），且经典 `ghp_` 令牌不支持 API 自助吊销（列/销端点 404/000，沙箱无 `gh` CLI）→ **必须由你在 GitHub 控制台吊销并重置**（见 §12）。
- 🔒 **沙箱 .gitignore 加固（防御纵深，建议执行）**：在沙箱补 `data/questions_real/_attachments/` 与真值 plaintext 的排除规则，防止未来任何误推把真 flag 带出去（远程已做，沙箱未做）。
- 📌 **公开仓以远程 `5a82feb` 为权威，停止任何沙箱→公开仓的推送企图**。后续功能迭代应在远程布局（根级 `tools/ verify/ web/`）下进行，而非沙箱嵌套布局。
- 🚧 **真正的能力短板**（与原 §7 P1 一致，与 git 无关）：web 真实靶机工具化、misc/vnctf 图像采样闭环——这才是下一阶段该投入的方向。

---

## 12. 🔴 P0 执行手册：吊销泄露 PAT（用户手动，沙箱无法自助）

> ✅ **已确认吊销**：2026-08-26 18:07 复探 `GET /user` 返回 **HTTP 401 Bad credentials**（此前为 200）→ 令牌 `[REDACTED-GITHUB-PAT]` 已失效，P0 凭证泄露风险闭环。本地 `.git/config` 明文 URL 此前已 strip。

**为什么必须手动**：经典 Personal Access Token（`ghp_` 前缀）不支持用自身调用 API 列/销（端点返回 404/000），且本沙箱无 `gh` CLI、已 strip 本地令牌。唯一可靠路径是 GitHub 控制台。

**步骤（约 1 分钟）**：
1. 登录 GitHub → 右上角头像 → **Settings** → 左下 **Developer settings** → **Personal access tokens** → **Tokens (classic)**。
2. 找到 `[REDACTED-GITHUB-PAT]`（账户 `TrueFurina` / `Dream`）→ 点 **Revoke / Delete**，确认。
3. 吊销后**立即重新生成一个新 token**（仅授予本仓库所需的最小权限：repo + 必要时 workflow），用于后续本机推送。
4. 本机更新远程地址（沙箱已 strip，本机若也含明文需同样处理）：
   ```bash
   git remote set-url origin https://github.com/TrueFurina/xihu-sword-ctf-agent.git
   # 推送时再用新 token / SSH / credential helper 鉴权，勿把明文 PAT 写回 URL
   ```
5. （可选）启用仓库 **Settings → Security → Secret scanning** 防护；本仓库已脱敏真值，但历史若曾含密钥需靠吊销兜底。

> 吊销前该 token 仍可读写你的公开仓库——**这是当前最高优先级的动作**，高于任何代码合并。

---

## 13. ✅ 沙箱 .gitignore 加固完成（2026-08-26 18:10，纵深防御真 flag 泄露）

> 执行结论：**已 commit `3e251d9`（master 顶端），门禁全过；磁盘 21 个真 flag 附件 100% 保留，仅从 git 索引移除。**

### 13.1 发现的真坑：嵌套 .gitignore 否定规则覆盖根规则
- 根 `.gitignore` 原本无 `_attachments/` 排除；补上 `ctf_agent/data/questions_real/_attachments/` 后，`git check-ignore` 仍报命中 `ctf_agent/.gitignore:109:!data/questions_real/**`（否定规则）→ **根规则被覆盖，真 flag 附件仍可跟踪**。
- 根因：`ctf_agent/.gitignore` 第 106–110 行有 `/data/*` + `!data/questions_real/` + `!data/questions_real/**`，**重新包含了整个真题集子树**（本意是保留真题集目录占位、排除运行时数据），但其 `**` 过于宽泛，连带把 `_attachments/` 真 flag 附件也 un-ignore 了。

### 13.2 修复（两文件协同）
- 根 `.gitignore` 新增 §14：`ctf_agent/data/questions_real/_attachments/` + `**/flag.txt` + `**/flag.png` + `**/*.flag`。
- `ctf_agent/.gitignore` 在 `!data/questions_real/**`（line 109）**之后**追加：
  ```
  # 真题集附件含明文真 flag，重新忽略以超越上面的 !data/questions_real/**
  data/questions_real/_attachments/
  data/questions_real/**/flag.txt
  data/questions_real/**/flag.png
  data/questions_real/**/*.flag
  ```
- 验证：`git check-ignore` 现命中 `ctf_agent/.gitignore:112:data/questions_real/_attachments/`；`git add -n` 报告路径被忽略；`git ls-files | grep _attachments` = 0（已不跟踪）。

### 13.3 执行动作（不删任何磁盘文件）
- `git rm --cached -r ctf_agent/data/questions_real/_attachments/`（21 个文件从索引移除，磁盘完好保留）。
- commit `3e251d9`，门禁：密钥扫描 ✅ / 诚实口径 ✅ / 租约单人模式放行 ✅。
- 验证：磁盘 `flag.txt`(238B)、`flag.png`(1.2MB) 时间戳 `Aug 26 09:51` 仍在；`git status --ignored` 显示 `!! ctf_agent/data/questions_real/_attachments/`（已忽略）。

### 13.4 角色重申
- 沙箱 `master=3e251d9`（原 a25173d + 加固）是**本地开发副本带明文真 flag**，**绝不可 push 到公开仓**。
- 公开仓权威主线仍为远程 `5a82feb`（已脱敏、带工程化文档）。

---

## 14. ✅ 本轮按优先级推进：web 工具化 P1–P3 全部落地（2026-08-26 18:2x）

> 用户指令"按照优先级一一来"。git 安全/治理线（P0 + .gitignore 加固）已在前序完成，本轮转入**真实能力短板**（honest KPI 最大缺口 web 真实赛事 0/10）。
> 全部在**沙箱本地**完成（不 push 公开仓），commit 经治理门禁（密钥扫描/诚实口径/租约/TOP0 总账）全过。

### 14.1 P1 — web_toolkit 补 SSRF 自动解分支（commit `a43a20e`）
- `ctf_agent/agents/web_toolkit.py`：新增 `_FALLBACK_SSRF`（纯 `httpx` GET，沙盒友好）+
  `build_fallback_script` dispatcher 加 SSRF 路由（关键词 ssrf/服务端请求伪造/内网/gopher/302/重定向/元数据/file://）。
- `ctf_agent/scripts/web_target_range.py`：新增 **web-011 SSRF 端点**（`?url=` 由服务端 urllib 代发抓取，`/web-011/flag` 返回 flag）。
- 端到端验证（用 stdlib urllib 做 httpx 等价桩，跑**未经修改的生成脚本**）：
  - ① 语法 OK ② dispatcher 路由 OK ③ **沙盒 AST 校验通过**（httpx/urllib.parse 在白名单，无 os/popen）④ 真实抓到 `flag{range_training_2026}` ✅。

### 14.2 P2 — 路径穿越兜底结构化抓 flag 闭环（commit `49c7083`）
- `_FALLBACK_TRAVERSAL` 命中 `flag{` 后由"print 全文"改为**结构化回传** `[FLAG_FOUND] vuln=path_traversal param=.. payload=.. flag=flag{..}`，供主 Agent 直接取值，避免空转。
- 验证（web-005 练兵端点）：输出 `[FLAG_FOUND] ... flag=flag{range_training_2026}` ✅。

### 14.3 P3 — vnctf 视觉采样闭环强化（commit `273b478`）
- 原 `verify_vnctf_flag.py` 是单题守卫；新增 **`verify_real_questions.py` 批量守卫**：扫描全部 15 题 / 20 附件，核验
  ① 题面 flag 字段为哈希占位（无 `flag{...}` 明文）② 附件磁盘存在 ③ 真值库 sha256 与台账一致。
- 验证结果：**0 明文 flag 回归、0 附件缺失、0 sha256 不匹配** ✅。
- 本轮实测澄清：真题集 JSON 的 `flag` 字段实为 **SHA-1/SHA-256 哈希占位**（40/64 hex），**非 `flag{...}` 明文**；
  明文真 flag 仅存于 `_attachments/` 磁盘文件（已 gitignore，§13 加固）——与 §11"沙箱带明文真 flag"指的一致（指附件层，非 JSON 层）。

### 14.4 诚实结论（本轮）
- web_toolkit 路由/兜底分支现已覆盖 **SSTI / 反序列化 / JWT / CMD / BACKUP / TRAVERSAL / SQLi / SSRF** 八类，且 TRAVERSAL 已结构化回传 flag。
- 但需清醒：**以上均为 self_authored 练兵场验证（不计入 KPI）**；"web 真实赛事 0/10"的 KPI 缺口，根因是**无开放赛事 + 无真实靶机**，工具链本身已不再是最短木板——下一步若要抬真实水位，须有真实赛题/靶机输入，或把兜底能力接到主 Agent 的真实 HTTP 交互链路上做实战验证。
- 沙箱三 commit（`a43a20e`/`49c7083`/`273b478`）**仅留本地**，因沙箱含明文真 flag 附件，按 §11 决策**永不强推公开仓**。

### 14.5 遗留（非阻塞）
- 残留旧进程 PID 5084 仍占用 9001 端口跑老代码（本轮回测时改用 9002 规避）；如需清理可在本机 `taskkill /PID 5084 /F`（不影响仓库）。
- 9001 端口的 stale server 是 localhost 训练靶机，无外部暴露风险，可留可清。

---

## 15. ✅ 主 Agent 真实 web 链路集成验证 + SSRF 命中未 break 缺陷修复（2026-08-26 20:4x）

> 用户"需要"= 把兜底接到主 Agent 真实 HTTP 链路上做实战验证。本轮不走孤立跑脚本，而是**复刻主 Agent 实际调度路径**。

### 15.1 验证路径（即生产代码）
- 主 Agent 的 web 兜底链路在 `ctf_agent/core/phases.py:226-237`：
  `build_web_fallback_script(ctx)` → `WebToolkit.build_fallback_script(url, desc)`
  → `agent.sandbox.run("python: {script}")`（`SubprocessExecutor`：AST 校验 + 子进程隔离）→ 解析 stdout `flag{`。
- 集成脚本 `.workbuddy/integration_web_chain.py`（已 gitignore）以**相同代码**对当前靶机跑 SSRF / 路径穿越两条链路。

### 15.2 抓出的真实缺陷
- **SSRF 模板命中 flag 后未 `break`**：继续打 `169.254.169.254`/`file://` 等会挂死的候选，108 个请求组合总时长超 30s 被沙盒强杀；非 TTY 下 stdout 缓冲未刷出 → 误判为空输出超时（exit_code=-9）。
- 这是 P1 回填 SSRF 时漏掉的（当时只孤立测了脚本、没经 dispatcher + 真实沙盒跑大写描述）。

### 15.3 修复与验证结果
- 修复：SSRF 模板加 `_hit` 标志，命中即 break（沙箱 `web_toolkit.py` commit `1a2cdcf`，公开仓副本 `E:/tmp/xihu_pub2/agents/web_toolkit.py` 同步修复，本回合无有效 PAT 未推送）。
- 重建验证（miniconda3 python，自带 httpx 0.27.2；managed 3.13.12 缺 httpx 故不可用）：
  - **SSRF=PASS**：`[ssrf-hit] param=url url=http://127.0.0.1:9002/web-011/flag -> flag{range_training_2026}`（exit 0 不超时）
  - **路径穿越=PASS**：`[FLAG_FOUND] vuln=path_traversal param=download payload=flag.txt flag=flag{range_training_2026}`
- 结论：**web 工具化 P1/P2 已通过主 Agent 真实链路闭环**（代码即 `phases.py` 两行），不再是"脚本自嗨"。

### 15.4 关键环境坑（可复用）
1. web 兜底脚本依赖 `httpx` → 跑真实链路须用**带 httpx 的 python**（本机 miniconda3 0.27.2；managed 3.13.12 无）。
2. `subprocess_executor.py` 用 `from sandbox.executor import ...` 顶层包导入 → 必须从 `ctf_agent/` 目录内运行（sandbox 为顶层包）才能 import。
3. Git-Bash 下 MSYS 路径转换会把 `/e/...`/`E:/...` 搞成 `E:\e\...` → 用 `MSYS_NO_PATHCONV=1` 或 `cd` 进目录用相对名。
4. 沙盒子进程 stdout 非 TTY 有缓冲，长循环被强杀会丢输出 → 兜底模板应在命中 flag 后**立即 break/退出**，既省时又保输出。

### 15.5 诚实结论
- 本轮把"web 工具化可用性"从*脚本级*推到*主 Agent 调度级*，是 P1/P2 的实质闭环。
- 但**仍为 self_authored 练兵场验证（不计入 KPI）**；公开仓 `main` 已含 SSRF 分支（c7dfc42），待补 break 修复后推送即与沙箱一致。
- 真正抬 web 真实水位（KPI）仍须真实赛题/靶机；工具链已非瓶颈。
