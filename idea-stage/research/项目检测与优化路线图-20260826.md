# 项目检测与优化路线图（2026-08-26 实测）

> 本文所有数字均来自今日磁盘实测 + 离线真值复跑，**非记忆/二手汇报**。

## 一、当前真实状态

### Git / 工作树（实测）
- 主仓库 `master` @ `58e5ea6`，基于 `origin/open-clean`（upstream 已 gone）
- **未提交改动 3 项**（均为合理工程能力，非治理文档）：
  - `eval/benchmark.py` + `run.py`：新增 `--presolve-skip` 开关——构造"必须走主 Agent"的子集，使主 Agent 改进可被 A/B 测量（因 14/15 被 presolve 直出时主 Agent 改进测不到）
  - `data/results/第Agent值守交接文档-20260825.md`：更新
- 多个 worktree 存在（`wt-continue`/`wt-d5`/`wt-p0closeout`），但**当前无 python 解题进程在跑**（仅 WorkBuddy 自身 node 进程）

### KPI 真实水位（今日复跑确认，零 LLM 调用离线验证）
- **真题集 `data/questions_real/` 15 道，确定性 presolve 管线真值匹配 = 15/15**（52s 复跑全绿）
  - `real_crypto_specialcurve2`：12.7s（factordb 分解 + PARI znlog）
  - `real_misc_vnctf_flag`：21.8s（网格重采样 + 视觉 LLM OCR 兜底 + sha256 校验）
- **但：全部 15 题由 presolve 静态分析器直出，主 Agent LLM 推理贡献 = 0**

### 两口径（不可混用，混用即漂移）
| 口径 | 数值 | 含义 |
|---|---|---|
| 闸门锚 `offline_verified`（严格真题口径，排除外部块） | **5** | 真实"离线验证"子集 |
| 真题集确定性管线覆盖 | **15/15** | presolve 主导的静态直出 |

### 工程基线
- pytest 9.1.1 就绪，`.venv` 可用
- 合并闸门已 fail-closed（`58e5ea6` 修 dirty_check 误杀），`--kpi-only` 全链路 PASS

## 二、核心矛盾（必须正视）

**第九轮作战计划（2026-08-25）的前提已完全失效：**
- 计划假设"决赛时间窗：8/31 晋级名单，9 月底决赛，剩约 4 周"，70% 火力打「主 Agent 提示词/LLM 循环攻坚」
- 事实：**比赛已彻底结束**，初赛 64 题 0 解出 0 提交已出局，现无开放赛事（平台 40403 无权）
- 继续照计划做 E1-E8 提示词实验 = **RDD 复发**（数字没动又堆 commit）

**能力与表象的鸿沟：**
- 15/15 是 presolve（静态规则/工具链）的功劳，不是"AI 推理能力"
- 真正体现智能的「主 Agent LLM 真推理」贡献 = 0
- 初赛真实战场 web 0/10 是真短板，且恰是 LLM 该发挥之处

## 三、"继续优化"的诚实路线图（按 ROI）

### ✅ 真杠杆（工具化，让 LLM 有真工具可用）
1. **Web 真靶机 + 主 Agent 工具化**（唯一能提"能力"的大杠杆）
   - 自建真靶机（真 sqlite/jinja2/PHP unserialize 语义），把主 Agent 从"正则认输"升级为"真执行"
   - 让主 Agent 能调用图像采样（vnctf 已验证）+ 真靶机执行，把 LLM 贡献从 0 推上去
   - 初赛 web 0/10 的短板由此攻破
   - ⚠️ 工程量大，且无赛事验证战场（比赛已结束）
2. **主 Agent 工具化补强**（presolve + 主 Agent 工具调用混合编排）
   - 当前 presolve 直出 15/15，但 LLM 在"需多步推理/独立攻击链"的题上仍是 0
   - 方向：让主 Agent 能编排现有工具层（crypto_auto/stego/web_request 等），而非只靠 presolve 单发

### 🟡 安全且立即可做（工程收口）
3. **收口当前未提交改动**：提交 `--presolve-skip` 测量开关 + 交接文档（今日复跑已确认 15/15 真实，应固化）
4. **git 收口**：清理非活跃 worktree、根目录净化、备份目录移出仓库

### 🔴 RDD 陷阱（不要做）
5. 继续 E1-E8 提示词/LLM 循环类实验：已到天花板（LLM 贡献 0，15/15 是 presolve 不是 LLM），数字不变又堆 commit = 复发回滚
6. 继续写"治理文档/复盘报告"：已够用，再写 = 复发

## 四、建议的下一步
- **立刻可做（零风险）**：收口未提交改动 + git 收口（资产固化）
- **需你拍板**：是否投入 Web 真靶机 + 主 Agent 工具化（唯一能真提"能力"的方向，但工程量大、无赛事验证）
