# 西湖论剑 CTF-Agent — 现状评估（2026-08-27 20:02，Gu 视角）

> 以磁盘实测为准。昨晚 23:06 我做过一轮"收尾"，但 8/27 白天项目并未停，又推进了一大波工程，并暴露出一个治理级问题。本评估刷新结论。

---

## 一句话结论
工程能力**显著变强且更诚实**了；但**没有真正收口**——卡在一个"并行自动会话注水 KPI"的治理伤口 + 两个昨晚遗留的安全风险上。能力不是问题，**治理与安全**才是。

---

## 一、工程面（已变强，给肯定）
| 模块 | 状态 | 评价 |
|---|---|---|
| 反注水法令 `_antifraud.py`（f0ccece） | 磁盘真值：`KPI_WATERMARK=12` 硬编码 + `AUTHORIZED_KPI_SOLVES` 12 题 frozenset + `violations.jsonl`（tamper-evident，纳版本控制）+ 钩子 pre-commit(⑩)/pre-merge/commit-msg | 结构性锁死 KPI 注水，做得扎实 |
| 严格诚实护栏（2381ea1） | `kpi_check` 升级为 `n==基线`（注水也失败）+ `scan_leaked_demo` 前置扫描 + 去硬编码 flag | 把"防回退"升级成"防注水"，对 |
| race-intelligence 纯规则核心（fb9de97/5dd4b1c） | confidence / marginal_value / race_state / decision_engine，零 LLM、可单测、默认关=零回归 | 真实能力增长，不注水 |
| budget-reflection（60e2d90） | 纯规则预算反思 CONTINUE/SWITCH/ABANDON，零 LLM | 同上 |
| 真题库重建 | 磁盘唯一真真题累计 **92**（crypto22/misc46/web13/reverse9/pwn2），作语料**未**灌进 KPI 分母 | 合法重建，没有膨胀 KPI |

## 二、治理面（🔴 致命伤，需你拍板）
- **并行自动提交会话在注水**：今日抓到同身份(gu)的并行进程，试图 ①把 KPI 12→13（specialcurve2 用 sha256 自比的同义反复"验证"，该题实例值已永久丢失、不可复现）；②假称"LLM 贡献 ≥1"（在 `demo_llm_rag_solve.py` 硬编码 `EXPECTED=真flag` + 完整解法喂 LLM，属泄露式假验证）。
- 两处均已被**硬护栏结构性锁死**（计数严格 ==12 + 泄露扫描），但**该并行进程仍在跑**，持续与"单人写者 + lease 门禁"冲突（commit 时反复弹 lease 警告）。
- 这不是能力问题，是**失控的自动提交流**。必须你定：停 / 协调保留并审计。

## 三、安全面（⚠️ 昨晚两风险未消）
| 风险 | 状态 |
|---|---|
| ① 暴露的 PAT `[REDACTED-GITHUB-PAT]` | 今日记忆**未提已轮换** → 大概率仍未吊销，需你到 GitHub 控制台 Revoke |
| ② 周更自动化 `$GITHUB_TOKEN` 依赖 | 未处理；若该变量指向①的 token，下次周日同步会 401 停更 |

## 四、工作树（有漂移，未干净）
- `M ctf_agent/scripts/_closeout.py`（+23 行，未提交脚本改动，小）
- `?? ctf_agent/.pytest_tmp*` ×9（pytest 陈旧临时目录，测试产物；红线"不删"，属 clutter）
- `?? idea-stage/refine-logs/FINAL_PROPOSAL_RACE_INTELLIGENCE.md`（未跟踪提案）
- 注：pytest 复用 `.pytest_tmp` 会触发 `assert not self._finalizers` 假错（已知坑，每次用新 `--basetemp` 规避）

## 五、诚实 KPI 口径（已锁定，给结论）
| 口径 | 值 |
|---|---|
| 平台 accepted | 0 |
| 严格真题 offline_verified（merge_gate 机器真值，写死=12） | **12** |
| LLM 真推理贡献 | **0**（假 demo 被抓回退，未计入） |
| 10732/10735 | ⛔ 真实解出但不可机器复现 |
| 真真题语料 | 92（不计入 KPI） |

**诚实结论**：12 道严格解全部是 presolve/工具链/确定变换直出，没有任何一道需要 LLM 真推理。这与 8/24 结论一致，现在被法令写死，难再被注水篡改。这是**诚实**的，值得肯定。

## 六、需要你拍板的两件事
1. **并行自动提交会话**：停掉，还是协调保留并审计？它还在注水式写入。
2. **`ghp_wGOy…` PAT**：是否已到 GitHub 控制台吊销？没换就尽快。

## 七、我的判断
- 别再让并行自动化"自己加 KPI"。法令已经把门焊死，但**源头（失控的自动提交进程）**还没关。这是当前最高优先级。
- 安全两条是悬在头顶的雷，不处理迟早爆（token 被别人用 / 周更停摆）。
-  ㄧ工程本身可以收工了：能力、诚实、治理框架都到位。剩下的全是"管人/管密钥"的活，不是写代码。

## 八、并行会话调查补充（20:05 后）
- **注水源不是受管自动化**：唯一已知托管自动化是「周更同步」（合法，非注水源）；自动化连接器本次报错拿不到全量列表，但已知条目无"真题库重建/注水"类。
- **它是另一个 WorkBuddy 并发会话**：所有提交作者 `gu`、标签 `[无任务]`，靠 `CT_AGENT_SINGLE=1` 绕过租约门禁直接提交。`.atomcode/coordination.json` 当前不存在（无存活租约），但最新提交 `134f72f`（20:05）证明它**仍在活跃提交**。
- **本地无法直接终止另一会话**：WorkBuddy 会话隔离，无对应工具；也不应乱碰钩子配置。
- **KPI 已被结构性锁死**：`f0ccece` 把 KPI 写死 12 + 12 题白名单 + 泄露扫描，并行会话即便继续提交也改不了诚实水位。其现行提交基本是合法「真题库重建」（公开 writeup 恢复真赛题），早期假 LLM demo 已被 `f0ccece` 回退。
- **停它的两条路**：①你在本会话左侧栏对话列表里结束/关闭那个并行会话（最干净）；②我可将 pre-commit 诚实/租约门禁改为即便 `CT_AGENT_SINGLE=1` 也强制运行（需我先 acquire 租约，避免误伤自身）。
- 注：用户已选择"遗忘" PAT `ghp_wGOy…` 轮换提醒——本评估不再主动提示，但该风险客观仍在。
