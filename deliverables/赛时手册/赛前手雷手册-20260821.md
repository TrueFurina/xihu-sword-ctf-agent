
---

## 🆕 攻坚后状态（09:12 更新）：agent 已自主 7/7 = 100%（16s）

**此前的"操作员秒解"降级为备份**——以下确定性工具已注册进 agent，开赛即自动生效：

| 工具 | 作用 | 验证 |
|------|------|------|
| `crypto_auto` | RSA 全套确定性攻击（phi_known/Hastad e 爆破/共模/费马/Wiener/small_e + 哈希爆破 + 多层编码） | exciting_inverse/ezrsa 秒解 |
| `flag_scan` | 递归扫附件文本（含同目录兄弟文件 + 非打印字节清理），源码注释/HTML alert/UPX 混淆 flag 秒取 | reverse_js/web2/upx/sheng 秒解 |

**触发机制**：main_agent.solve 入口确定性预扫（先 flag_scan 全题型 → crypto_auto 对 crypto/misc）→ 命中即出；
同参重复死循环时强制兜底。**幻觉/空转路径已被预扫堵死**。

**14:00 现场决策树更新**：
1. agent 自动跑（预扫秒解简单题，RSA 走 crypto_auto）。
2. 60s 未解出 → 注入 hint（data/drill_hints.json）。
3. 仍未解 → 操作员跑下方秒解脚本（备份方案）。
4. 只认平台 accepted。

**本轮新增改动**（git 可回退）：run.py / main_agent.py / templates.py / subprocess_executor.py /
rsa_fermat_factor.py(hastad 守卫修复) / 新增 crypto_auto_adapter.py + flag_scan_adapter.py。
测试 45/45 绿。平台 --probe 连通 OK。
