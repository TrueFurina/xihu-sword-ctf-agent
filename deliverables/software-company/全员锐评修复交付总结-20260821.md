# 西湖论剑 CTF-Agent · 全员锐评修复交付总结

> **团队**：software-ctfagent（软件开发团队全员协作）
> **日期**：2026-08-21（正式赛当日赛后 · 决赛备战起点）
> **流程**：架构师极致锐评 → 产品经理竞赛锐评 → 工程师批量修复（T01-T04）→ QA 独立回归 → 工程师收尾
> **HEAD**：`c703e8f` | **测试**：138 passed / 1 skipped | **密钥扫描**：exit 0 全绿

---

## 一、TL;DR

**双人锐评 → 4 组修复 → 独立验证全绿。** 架构"组件级 85 分、系统级不及格"的根因（正式赛 0/62：附件不下载/LLM 空转/提交吞错）已全部定位，P0 安全与稳定性问题（AccessKey 泄露、墙钟资源泄漏、预扫未进真实链路、429 丢题）已修复落库，测试从 51 增至 **138 全绿**。

---

## 二、锐评核心结论（双视角）

| 视角 | 一句话结论 |
|------|-----------|
| 架构师（高见远） | **温室冠军**：确定性引擎（数学 8/9）真实存在但从未在正式赛闭环触发；组件级 85 分、系统级不及格 |
| 产品经理（许清楚） | **技术 A 级、工程 F 级**：正式赛真实 0/62 解出（赛前 89%/92% 是温室数据），六层根因，最隐蔽为提交链路 fail-open 吞错 |

### 正式赛 0/62 六层根因（已全部定位并修复或纳入排期）
1. 平台 API 字段错配 → 附件不下载/靶机不解析/题面为空（已修复：e2e 验证脚本 + 附件解析）
2. 87% 时间 LLM 纯推理空转（已修复：presolve 确定性预扫统一入口 + 工具调用前置）
3. 同题反复 8-12 次无效循环（已修复：失败题重试队列 + 429 退避 + 墙钟全量取消）
4. 赛前度量虚假繁荣 89%/92% vs 实战 0%（已定音：诚实分层口径 33%/本地组合/测试赛 7/7）
5. 分类器无数据全归 misc（已修复：32 题分类回归 web 23/32，纠正 75% misc 误判）
6. **提交链路 fail-open 吞错**：10719 正确 flag 5 次提交被吞→熔断→静默丢弃（已修复：请求层故障 request_failed 标记与 flag 错误分离，熔断不计入 flag 错误）

---

## 三、修复交付（T01-T04）

### T01 安全与配置治理 ✅
- **AccessKey 治理**：`${XIHU_PLATFORM_AK}...` 全库清零（工作区）；`.gitignore` 补 `data/results/*.md`、`data/results_backup*/`；新增 `scripts/_scan_secrets.py`（密钥扫描门禁，大文件只读头 2MB）+ pre-commit hook 已安装
- **config 单一事实源**：dataclass / from_env / _resolve_provider_defaults 三处统一默认 **baidu**，删除矛盾注释

### T02 竞速/墙钟资源泄漏修复 ✅
- run.py race 墙钟 try/except/finally + `_cancel_all()`（全量 cancel + gather return_exceptions）
- subprocess 补捕 `asyncio.CancelledError` → `_kill`（Windows `taskkill /T /F` 杀进程树）
- llm/client 新增 async 包装（to_thread 不阻塞事件循环），`_post_chat` 返回 (content, usage)
- token 记账：ContextVar 按任务累计 plan/reason/supervise 全部调用，替换并发失真的模块级 `_LAST_USAGE`
- math_engine 总 deadline + `_brute_5digit`/`_recurse_zip` 20s 时间预算

### T03 预扫收敛 + poller 429 ✅
- **core/presolve.py 统一入口**：flag_scan → crypto_auto → math_engine → 关键词，带 `[presolve:<engine>]` 日志，await 前去重标记（同附件只嗅探 1 次）；run.py / main_agent.py / race 第 0 号选手全部改调
- poller：失败题不再"处理前即 processed 永久丢题"，进重试队列（2-5min × 3）；run_forever 429 阶梯退避（30/60/120/300s）
- dasctf：429 退避上限 62s→15s；AsyncClient 按事件循环复用；submit_flag `int()` 加 isdigit 防护
- `--firstblood` 复用 build_platform_solver（下载附件+注入靶机）

### T04 死代码与目录清理 ✅
- `scheduler/rate_limiter.py` + `model_router.py` 删除（生产零引用，测试改道 llm/client）
- scripts 顶层 70+ → 20 个，其余归档 `scripts/_archive_legacy/`（git 历史保留）；tempdir/tmp 清理
- 看板持久化：`dashboard_tasks.json` 原子写 + `_run_all` 运行锁

### 收尾（QA 后）✅
- git 状态修正（AD 幻影恢复）+ 7 项在飞改动落库 `b81a401`（86 files, +428/-10593）
- P1 收敛：race 第 0 号选手改走 presolve 去重 `c703e8f`（新增 test_race_presolve_dedup）
- P2：setup.sh 悬空引用修正、`_p0_diff.txt` 含明文 key 文件删除

---

## 四、QA 独立验证（严过关）

- **判定：可交付**。全量测试 **136 → 138 passed / 1 skipped**（独立重跑，未采信工程师自测）
- 6 个重点测试文件抽查全部为真实行为测试（断言与修复意图一致）
- 验证矩阵：T01-T04 + 看板持久化逐项核验通过（证据详见 `QA验证报告-20260821.md`）
- 路由判定：SourceBug 无阻断性缺陷；TestBug 无；唯一提交阻断为 git 操作问题（已由工程师修复）

---

## 五、commit 链（赛后 12+ 个）

| commit | 内容 |
|--------|------|
| `c703e8f` | fix(赛后P1): race第0号选手改走presolve去重 |
| `b81a401` | fix(赛后QA): 在飞改动落库+归档清理 |
| `3c9e6a4` | fix(赛后T02): llm/client usage 解包 + scheduler 死代码删除 |
| `2009aa6` | e2e平台验证接入setup.sh(--e2e) + 32题分类回归 |
| `3449e16` | scripts冗余归档 + 赛时真题skill沉淀4个 |
| `bd9e0cf` | ultra档位实测存活源 + presolve统一层测试 |
| `eecd74f` | FakeAsyncClient兼容follow_redirects |
| `28c77da` | dasctf AsyncClient按事件循环复用 |
| `c4fe797` | 详情拉取失败区分重试 |
| `1fee8bd` | poller失败题重试队列 + gitignore补充 |
| `24e6853` | 全员锐评修复收口（数据链路/沙盒/看板/瘦身/测试） |
| `d4b5a6c` | P0-2墙钟/取消语义回归测试 |
| `0774a16` | 真实token记账+墙钟取消+异步包装 |
| … | 其余见 git log |

---

## 六、遗留事项（需用户决策/外部条件）

| # | 事项 | 类型 | 建议 |
|---|------|------|------|
| 1 | **AccessKey 轮换**：git 历史仍含旧 key（`${XIHU_PLATFORM_AK}...`），工作区已清零 | 🔴 安全 | 平台侧轮换；决赛前仓库不外发则历史清洗（filter-branch/BFG）可暂缓，外发前必须清洗 |
| 2 | **`_p0_diff.txt` 已删除**，但其 5 个 sk- API Key（TOKENHUB/MOONSHOT/SILICONFLOW/SENSENOVA/TOKENROUTER）曾在文件里出现 | 🔴 安全 | 相关平台侧轮换这些 key（若仍在使用） |
| 3 | **LLM 多源健康验证**（P0-4）：千帆主源 + ≥1 备用源充值/开付费 | 🟡 环境 | 复跑 `scripts/_llm_health_probe.py`，至少 1 源返回 200 |
| 4 | **真实平台 e2e 演练**（P0-3 验收②）：附件下载→预扫命中→提交 accepted | 🟡 环境 | 平台可用后跑 `setup.sh --e2e` 或 `scripts/_e2e_verify.py` |
| 5 | **32 题真题回归**（P0-5）：分类正确率 ≥90%，dry-run 目标解出 5-8 道 | 🟡 排期 | 决赛前 4 周内 |
| 6 | **答辩三件套**（P0-6）：创新说明书 + 演示视频 + 诚实解出率报告（分层口径 33%/组合/7/7） | 🟡 排期 | 决赛前 |
| 7 | **晋级资格核实**（P2-5）：0 解出按常规规则是否仍有决赛资格 | ⚪ 战略 | 立即核实官方规则后再排资源优先级 |

---

## 七、决赛备战要点（口径红线）

1. **对外解出率口径**：只讲三件事——① 测试赛 7/7 满分（注明"同源语料"）；② 模型自主 2/6=33%（诚实口径）；③ 正式赛 0/62（主动说明 + 修复证据链）
2. **89%/92%/100% 一律不可直接引用**，必须加"本地真库题组合水位"限定词
3. **创新点主打**：双矩阵异构竞速（确定性引擎 8/9 实打实）+ 步骤级校验/幻觉拦截 + 提交链路防幻觉熔断——均有日志/代码佐证
4. **决赛铁律**：改码必重启 + e2e 验证为唯一判据；赛前 48h 代码冻结

---

*交付方：软件开发团队（齐活林统筹 · 高见远/许清楚/寇豆码/严过关）· 2026-08-21*
