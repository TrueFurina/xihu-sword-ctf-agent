# 西湖论剑 CTF-Agent · 遗留事项跟进确认（2026-08-21 19:40）

> 场景：用户指令"现在应该换了，都确认跟进"——逐项核实 7 项遗留事项现状，能自动验证的已实测，需人工的给出操作指引。

## 一、实测结果汇总（本轮新跑）

### 1. LLM 多源健康（P0-4）✅ 达标
`scripts/_preflight.py` 实测：**可用 7/11 源**，已满足"≥1 源 200"验收（实际 5 源 200）：

| 源 | 状态 | 说明 |
|----|------|------|
| 百炼 glm-4.7 | ✅ 200 | 可用 |
| 智谱 glm | ✅ 200 | 可用 |
| 讯飞星火 lite | ✅ 200 | 可用 |
| 月之暗面 kimi-k2.6 | ✅ 200 | 可用 |
| 字节豆包 doubao-seed | ✅ 200 | 可用 |
| deepseek chat/reasoner | ❌ 402 | 余额不足 → 需充值 |
| qwen3.7-plus | ❌ 403 | 免费额度耗尽 |
| siliconflow | ❌ 402 | 余额不足 |
| tokenhub | ❌ 402 | 免费配额耗尽 |

> ⚠️ 注意：config 默认 provider=baidu，preflight 显示 `CTF_AGENT_LLM_PROVIDER=baidu` 但 LLM 探针里百炼 glm-4.7 命中的是 qwen provider 端点——**需确认 baidu provider 的实际可用端点**，决赛前统一（见遗留 8）。

### 2. 平台连通性 ✅
`pro.dasctf.com` 连通 OK（token 长度 40），当前 0 题（初赛已结束未开放，符合预期）。`_e2e_verify.py --all` 输出"平台无题，决赛前 1h 再跑"——脚本就绪。

### 3. 32 题真题 presolve 回归（P0-5 第一步）📊 关键发现
`_dryrun_race.py --presolve-only` 全量跑完（1m47s，零 LLM 成本）：

**总命中 1/32 = 3.1%（且为乱码误报 → 真实 ≈ 0%）**

| 题型 | 题数 | 命中 | 结论 |
|------|------|------|------|
| web | 23 | 0 | **结构性主因**：真题 72% 是 web 靶机题，本地无靶机必 miss；正式赛"000 连接失败"即此 |
| crypto | 2 | 0 | 真题是 PKCS#1/e=65536 类，本地 skill 全是 RSA 小 e/费马/Wiener → 能力错配 |
| reverse | 3 | 0 | 大文件分析弱 |
| misc | 3 | 1(误报) | 10734 "flag^galf" XOR 类，presolve 提取到 XOR 后字节串，非真实 flag |
| pwn | 1 | 0 | 需交互 |

**结论**：89%（本地真库组合）vs 真题 0% 的剪刀差被实测钉死。决赛若不补 **web 靶机交互 + PKCS#1/high-exponent 攻击链**，0 解出必然重演。

## 二、7 项遗留事项逐项状态

| # | 事项 | 状态 | 下一步 |
|---|------|------|--------|
| 1 | AccessKey 轮换（git 历史含 `${XIHU_PLATFORM_AK}`） | ⚠️ 待人工 | **平台侧轮换 AccessKey**；git 历史清洗命令已备（见下） |
| 2 | `_p0_diff.txt` 5 个 sk- Key 轮换 | ⚠️ 待人工 | 文件已删+gitignore；涉及的 TOKENHUB/MOONSHOT/SILICONFLOW/SENSENOVA/TOKENROUTER 平台侧轮换（若仍在使用） |
| 3 | LLM 多源健康 | ✅ 已达标 | 5 源 200；建议给 deepseek 充值（主源备选） |
| 4 | 真实平台 e2e 演练 | ⏳ 等平台 | 平台开赛/决赛前 1h 跑 `setup.sh --e2e` |
| 5 | 32 题真题回归 | 🔄 已启动 | presolve 层已跑完（≈0%）；下一步跑全链路 dry-run（含 LLM），**并优先补 web 靶机交互 + 2 个 crypto skill** |
| 6 | 答辩三件套 | ⏳ 未开始 | 决赛前 4 周内做（创新说明书/演示视频/诚实解出率报告） |
| 7 | 晋级资格核实 | ✅ 已核实 | **官方规则：初赛综合成绩排名前 12 晋级线下决赛，8/31 前公布名单**。0/62 解出下晋级概率极低——建议立即确认队伍实际排名/成绩单 |

## 三、git 历史密钥清洗方案（待用户决策后执行）

定位：`git log --all -S "${XIHU_PLATFORM_AK}"` → commit `0774a16` 引入（后被 1aa7999 脱敏，但历史仍含明文）。

方案 A（推荐，仓库不外发时）：**仅轮换 AccessKey，不动 git 历史**——决赛仓库绝不外发/上传即安全。
方案 B（仓库需外发/给评委看代码时）：BFG 清洗：
```bash
# 备份后执行（在 ctf_agent 仓库根目录）
git clone --mirror . /tmp/ctf_agent_mirror.git
cd /tmp/ctf_agent_mirror.git
# 用 BFG（需 java）：替换 ${XIHU_PLATFORM_AK}... 整段
java -jar bfg.jar --replace-text /tmp/replacements.txt --no-blob-protection .
# replacements.txt 一行：${XIHU_PLATFORM_AK_2}==>AK_LIVE_REDACTED
git reflog expire --expire=now --all && git gc --prune=now --aggressive
```
⚠️ 会重写全部 commit hash，需协调并发会话（另一会话也在提交）；**确认所有副本都已轮换再执行**。

## 四、决赛前必须补的能力（本次回归证明）

| 优先级 | 能力 | 依据 |
|--------|------|------|
| P0 | **web 靶机交互**（web_target_interact：HTTP 000 诊断/端点注入/模板化攻击） | 23/32 是 web 靶机题，0 解出主因 |
| P0 | **crypto PKCS#1 padding oracle + e=2^16** | 正式赛 2 题 crypto 全 miss，本地 skill 错配 |
| P1 | misc bigfile 提速 + reverse 工具深化 | 各 3 题，1 题误报需重做 |

*核实人：团队主理人 · 2026-08-21 · 依据为实测输出（preflight/llm probe/e2e/dryrun）*
