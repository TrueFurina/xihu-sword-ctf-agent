# 西湖论剑 初赛续攻诊断（2026-08-21 17:00 后）

> 指令：「做出所有题目才停下」。结论：**初赛窗口（14:00–17:00）已结束，本 agent 实际 0/62 提交 accepted**，平台现已关闭（提交返回 `40403 无权操作`），无法再交卷。

## 一、当前战果（ground truth，来自平台 hasSolved + 提交回执）
- 平台总题 **62**，本 agent **提交 accepted = 0**。
- 列表中显示「已解 2」（CRYPTO-01/02）为**平台标记**，非本 agent 提交（见 2026-08-21 复盘：agent 全程 0 有效提交）。
- 60 题未解（crypto 7 / misc 30 / pwn 7 / reverse 9 / web 7）。

## 二、为什么一道都没解出来（三重阻断，已逐一实证）
1. **LLM 密钥集体失效（致命）**
   - `deepseek-chat` → `HTTP 401 invalid_iam_token`（`DEEPSEEK_API_KEY` 已失效/错误）。
   - `mimo/glm/xfyun/tokenhub/ark/siliconflow/moonshot/sensenova/qwen` → 全部 `invalid_iam_token`。
   - **唯一存活源：`baidu` ernie-3.5 / ernie-4.0-8k-preview**（`QIANFAN_API_KEY` 有效）。
   - 即：赛时配置「主源 deepseek」实为死链 → agent 全程 0 次有效 LLM 推理。
2. **Baidu 自由文本导致 main_agent 死循环（近因）**
   - `core/main_agent.py` 的 `_plan` 期望 LLM 返回**结构化 JSON action**；ernie 返回自然语言 → 每步被解析为 `reason` 空转 → `stuck_count≥2` → 监督 give_up → `stuck_loop`。
   - 实测：任何需要推理的题都在此路径 5–44s 内以 `stuck_loop` 退出。
3. **平台 17:00 已关**
   - `GET challenges` / `POST submit` 均返回 `code=40403 无权操作` → 任何迟交/补交均不可行。

## 三、我本会话实际做了什么
- 探针拉取平台真实状态（62 题 / 0 本-agent 解出）。
- 逐一实测 11 个 provider 密钥：仅 baidu 可用，其余全 `invalid_iam_token`。
- 启动 3 套驱动（`solve_all` 深seek版/`solve_det` 确定性版/`solve_local` 本地缓存版）：
  - `solve_all`(deepseek) → 全 `stuck_loop`（密钥死链）。
  - `solve_det`/`solve_local` → MathEngineMatrix + 递归 flag 扫描，**未命中任何真实 flag**（仅 CSS/二进制噪声假阳性，已加 `is_clean_flag` 过滤）。
- 结论：CTF 赛题不内嵌明文 flag，**无 LLM 推理 = 无法解题**；确定性引擎在此 60 题上 0 命中。

## 四、要真正「全解」的可行路径（决赛 / 练习复用）
1. **换有效 LLM 密钥**：更新注册表/环境变量 `DEEPSEEK_API_KEY`（或任一白名单 provider：qwen/mimo/glm/tokenhub/ark 等）。当前仅 baidu 活。
2. **修 Baidu 兼容**：在 `main_agent._plan` 加 JSON 提取/重试，或改路由到支持 tool-calling 的模型，消除 `stuck_loop`。
3. **决赛前端到端验证**：用 `ctfplatform.dasctf` 跑 `get_access` 字段解析 + solver 无条件拿 detail，复用上方复盘「决赛 P0 必做」。
4. **离线复算**：`data/race_attachments/`(30 题) + `data/attachments/platform_archive/`(15 题) 已缓存，可本地跑 MathEngineMatrix/flag 扫描。

## 五、当前遗留进程
- `solve_local.py`（收紧 flag 扫描）仍在后台跑，纯本地、不触平台、无害。
- 其余 compete / baidu / det 驱动已自退或空转。

**一句话**：初赛已无法补交；核心阻断是「LLM 密钥全死 + Baidu 循环 bug + 平台关闭」。给一把有效密钥 + 修 Baidu 解析，agent 即可在决赛/练习场真正开解。我已就此写好修复方案，需要你确认是否现在动手改 `main_agent` 与换密钥。
