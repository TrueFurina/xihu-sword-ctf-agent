# 西湖论剑 CTF-Agent 第五轮修复作战计划

> 制定人：Gu（指导老师视角）· 2026-08-22 22:40
> 唯一标准：**真实解出数** · **比赛第一名** · **最短解题时间**
> 全部基于磁盘实测（代码 + dryrun 数据），非整改声明。凡「已修复」附 file:line 证据。

---

## 0. 第五轮验收：钉死真相

| 上轮四根杠杆 | 实测状态 | 判定 |
|---|---|---|
| ① benchmark 全量跑 ≥30 题 | `benchmark_report.json` 仍 `total:1, solved:1`（caesar 572ms）；但 dryrun 已实测出**真实 LLM 基线 = 0 解出**（见 0.2） | ❌ 官方报告未更新 |
| ② race_strategy 接进 run.py | `run.py:560/596` 只 `import plan_challenges`（排序）；`RaceScheduler` 沉溺保护**仍是死代码** | ⚠️ 半接 |
| ③ 修 RSA 映射 | 未改；定性已变（见 0.1） | ⚠️ 待重定性 |
| ④ 轮询退避 + presolve 并发 | presolve **已 6 路并发**（`presolve.py:286-296`）✅；轮询 `fast_interval<30s` 仍焊死（`poller.py:227-230`）❌ | ⚠️ 一半 |

**已修复（本轮真落地）：** `_emergency.py` 明文 flag 已删（`known_flags={}`）✅ · benchmark 代码已接真链（`eval/benchmark.py:171`）✅ · 测试 204→**211** ✅

---

## 0.1 事实纠正：RSA 路由不是上轮说的「全错」

`skills/rsa_fermat_factor.py` 的 `run()`（第 54-129 行）是**攻击分发器**，内部 auto-detect 处理 `d_known/phi_known/multi_n/common_modulus/small_e/wiener/hastad/fermat`。所以「`wiener`→rsa_fermat_factor」**功能上没坏**，坏的是**技能名撒谎**（叫费马实为全能）。

**真正错误的映射**（`prompts.py` skill_map 实测逐条比对）：

| 关键词 | 现映射 | 正确处理 |
|---|---|---|
| `栅栏` `培根` | `base64_multilayer` | 删除（rail fence/bacon ≠ base64） |
| `rot13` `rot` | `base64_multilayer` | → `caesar_bruteforce` |
| `伪加密` | `zip_filename_chain_decode` | → `misc_zip_fake_encryption` |
| `明文攻击` | `crypto_coppersmith` | 删除 |
| `js合并` | `web_jwt_prototype` | 删除 |
| `二次编码` | `web_second_order_injection` | 删除 |
| `逆元` `inverse` | `rsa_fermat_factor` | 删除或降权 |

**另一层真相**：`infer_skill_require`（`prompts.py:214-227`）**永远 return None**，唯一作用是 `skill_manager.load()` 副作用（关键词命中→预加载 skill 进工具集），不替 LLM 做决策。所以错误映射的真实危害 =「预加载无关 skill 烧 context」，不是「路由到错误答案」。

---

## 0.2 关键补充：真实能力基线已被并行会话测出 = 0 解出（不是 17%，不是 n=1 的 100%）

`data/results/dryrun_*.json` 记录了真实 LLM 全链路实测（M2），结论比 benchmark_report.json 的 n=1 硬得多：

1. **真实解出 = 0**。raw=16 里有 **3 个假阳性**，全是 presolve 确定性扫描产出的垃圾，却标 `validated:true`：
   - `10716` → `flag{--fa:""}`（**joomla 包里 FontAwesome 的 CSS 变量 `--fa:""` 被扫成 flag**）
   - `10734` → `ctf{Q\x06D+,{?rv}`（含控制字符）
   - `10735` → `flag{C-02}`（占位符）
2. **20 题 budget_exceeded 是级联饿死，非能力**：9 道 web CMS 题（joomla/wordpress/drupal/ghost/cmsms）每题烧满 180s 超时=1620s，叠加 2 道工具失败，全局预算耗尽 → 剩余 20 题 LLM 根本没轮到（`llm_time_s≈0`）。
3. **真瓶颈（按证据排序）**：① 墙钟超时太短（150-180s）砍死难题；② 全局预算被 web CMS 题榨干造成级联饿死；③ baidu 千帆对 web CVE 侦察能力弱；④ presolve 假阳性污染口径。

**这一条推翻了我原计划「P0-1 = 跑 benchmark 拿真数」的前提**——真数已经有了（0），真正的 P0 是「修口径 + 修真瓶颈」，不是「测」。

---

## 1. 总原则（三句铁律）

1. **唯一指标 = 真实解出数**，当前基线 **0**。所有优化以「从 0 往上爬」为唯一方向。
2. **只做影响解出数的事**。新 skill/文档/启发式冻结，直到 P0 全部完成。
3. **每个改动被 dryrun/benchmark 真题回归审判**，解出数不升反降的改动回滚。

---

## 2. 修复清单（按序，前一项完成才进下一项）

### P0-1 修 presolve 假阳性口径 + 加答案护栏（先止血，否则一切数字都不可信）

**现状（实锤）**：`flag{--fa:""}`、`ctf{Q\x06D+,{?rv}`、`flag{C-02}` 三个垃圾被标 `validated:true` 计入解出。根因是 presolve 的 `_passes_answer_check` 太松——只查 `flag\{[^}]+}` 格式，不查内容合法性。

**目标**：假 flag 进不了 validated，真 flag 不漏。

**步骤**：
1. 在 `core/presolve.py` 的 `_passes_answer_check` 加**内容合法性白名单**：
   - 拒绝含控制字符（`\x00-\x1f\x7f`）、含引号/冒号/CSS 片段（`--fa`、`""`、`url(`）的候选；
   - 候选必须命中 `[A-Za-z0-9_{}-]+` 且长度 4-64；
   - `validated` 只在**候选通过内容白名单**后才置 true。
2. 对 `_try_desc_answer`（`presolve.py:311-342`）加闸：`validate_locally=False`（正式赛 `run.py:628`）时**直接禁用**该路，无 ground truth 不许产出候选。
3. 回扫 3 个垃圾题，断言重跑后它们不再 `validated:true`。
4. 加测试：喂 `flag{--fa:""}`、`ctf{Q\x06D+}` 等垃圾，断言 presolve 返回 None。

**验收**：dryrun 重跑后 `validated:true` 的 flag 全部是真 flag（无控制字符/CSS/占位符）；相关单测绿。

---

### P0-2 修墙钟超时双向 + 全局预算级联饿死（真瓶颈① ②，直接决定解出数）

**现状**：`_race_wc=300`（`run.py:439`）一刀切，但 dryrun 显示实际超时是 150-180s（`CTF_AGENT_PER_Q_WALLCLOCK` 等 env 生效），web CMS 题每题烧满超时榨干全局预算 → 20 题饿死。

**目标**：难题给足时间、简单题快速止损、预算不被单一类题目榨干。

**步骤**：
1. **难度分级超时**（接 `race_strategy.challenge_timeout`）：简单题 90s、难题 600s（web 源码审计/CVE 侦察需要远大于 180s）、重访 300s。
2. **预算隔离**：给每个 category（web/pwn/reverse/crypto/misc）设独立预算配额，web 题烧满预算不再饿死 crypto/misc 题——消灭「9 题 web 榨干全局 → 20 题饿死」。
3. **清死 env**：统一 `CTF_AGENT_PER_Q_WALLCLOCK` 与 `CTF_AGENT_RACE_WALLCLOCK` 的语义，只留一个读入口，或让 `challenge_timeout` 真正接管。
4. 加测试：构造「web 题烧满预算」场景，断言 crypto 题仍能正常求解（不被饿死）。

**验收**：dryrun 里 `budget_exceeded` 从 20 降到个位数；难题不再 150s 被砍；解出数从 0 开始有真实进账。

---

### P0-3 跑公平的真实 benchmark（真数刷新 + 定位下一瓶颈）

**现状**：`benchmark_report.json` n=1 没更新；真数（0）散落在 dryrun 里，没进正式报告。

**目标**：P0-1/P0-2 修完后，重跑全量，产出可信真数，替换 n=1 与 17% 两个过期数字。

**步骤**：
1. 附件完整性预检（排掉「缺附件」非能力性失败）：
   ```bash
   cd E:/Program/西湖论剑/ctf_agent
   .venv/Scripts/python.exe -c "
   import json, os, glob
   for f in glob.glob('data/questions/*/*.json'):
       q=json.load(open(f,encoding='utf-8'))
       miss=[a for a in q.get('attachments',[]) if not os.path.exists(a)]
       if miss: print(q['id'],'缺附件:',miss)
   "
   ```
2. LLM 烟测定死 provider（`scripts/_llm_health_probe.py`），先 crypto/misc 后 web/pwn。
3. 全量：`.venv/Scripts/python.exe -m eval.benchmark --questions-dir data/questions`。
4. 把真数回填《诚实水位声明》，删除 17%(1/6) 与 n=1。

**验收**：`benchmark_report.json` 的 `total≥30` 且 `solved` 为**可信真数**（无假阳性）；文档里不再有 17% 与 n=1。

---

### P0-4 修 7 组错误映射 + 技能名去谎（10 分钟，与上面并行）

**现状**：见 0.1 表。

**步骤**：
1. 修真错误映射：删 `栅栏/培根/明文攻击/js合并/二次编码/逆元`；`rot13/rot→caesar_bruteforce`；`伪加密→misc_zip_fake_encryption`。
2. 技能名去谎：`rsa_fermat_factor` → `rsa_attack`（改 .py/.json/文件名/引用/测试），或先加注释止血（决赛在即选后者，改名进 P2）。
3. 加路由完整性测试：断言 skill_map 所有 value ∈ `skills/` 真实文件，关键词无已删项。

**验收**：`grep` 确认 7 组错误映射消失；路由完整性测试绿。

---

### P1-5 沉溺保护真接线（RaceScheduler 不再是死代码）

**现状**：`run.py` 只用 `plan_challenges`（排序），`RaceScheduler.should_stop/on_timeout/revisit`（`race_strategy.py:55-106`）死代码。

**步骤**：
1. 竞速循环用 `RaceScheduler` 替代裸 `_race_wc`：超时 → `on_timeout` 入重访队列（`max_revisit=2`）→ 切下一题；队列空 → 拉重访题再战。
2. 加测试：假题 5 分钟不解，断言触发 on_timeout 且换题。

**验收**：`run.py` 有 `RaceScheduler` 真实调用；超时换题测试绿。

---

### P1-6 配置单一事实源（provider-model 对齐）

**现状**：env `baidu` / 代码默认 `baidu`（`config.py:116`）/ 记忆 `deepseek`；`mid_model=qwen3.8-27b`（`config.py:123`）与 baidu 千帆同源冲突（打 qwen 模型名到千帆端点会 404）。

**步骤**：烟测定死 provider → 三处统一（config/MEMORY.md/注册表）；修 `mid_model` 为同源模型；启动打印五行生效快照。

**验收**：`llm_provider` 与 `light/mid/heavy` 同家族；启动日志可见生效快照。

---

### P1-7 轮询首杀期指数退避（破「最短时间」）

**现状**：`poller.py:227-230` 把 `fast_interval<30s` 焊死——首杀期平均 15s 纯等待。

**步骤**：放开 `fast_interval` 下限到 5s，但**必须同时**加：① 每请求 ±20% 随机抖动；② 首杀期请求预算上限（10 分钟 ≤40 次），耗尽降频。保留 429/403 阶梯退避。

**验收**：`fast_interval=5` 不再被钳；预算耗尽自动降频；测试绿。

---

### P2-8 scripts 清理 · P2-9 goal_directive 瘦身（赛前收尾，不阻塞 P0）

- **P2-8**：44 个 `_` 脚本，逐个「泛化进 skill/presolve 了吗」——是则删，否则提炼或归档 `scripts/_archive_legacy/`；带题号脚本优先（决赛清点不留「预知题号」嫌疑）。
- **P2-9**：`goal_directive.py` 338 行塞 prompt → 抽 R1-R6 五条核心进 prompt，其余降级按需注入卡片。

---

## 3. 执行顺序与依赖

```
P0-1 假阳性口径 ──┐
P0-2 超时+预算 ───┼──→ P0-3 公平 benchmark（真数刷新）
P0-4 映射修正(并行) ┘        │
                             ▼
P1-5 沉溺接线 ← P1-6 配置对齐 ← P1-7 轮询退避
                             │
                     P2-8/9 赛前收尾
```

- **P0-1/P0-2 是地基**：不修口径和预算，P0-3 跑出来的数字仍是垃圾。
- **P0-4 可全程并行**：10 分钟，独立无依赖。
- **P1-7 有回归风险**：改轮询可能再触发 403，必须带测试 + 小流量试跑。

## 4. Definition of Done（全部满足才收口）

1. dryrun 中 `validated:true` 的 flag 无假阳性（无控制字符/CSS/占位符）。
2. `budget_exceeded` 级联饿死消灭，难题不再被 150s 砍死。
3. `benchmark_report.json` 真数 total≥30 且可信；17% 与 n=1 从文档删除。
4. `prompts.py` 无 7 组错误映射；`run.py` 有 `RaceScheduler` 真实调用。
5. `config.py` provider 与模型同源；启动打印生效快照。
6. `pytest` 全绿，新增口径/沉溺/路由完整性测试。
7. `scripts/` `_` 前缀 ≤10。

## 5. 一句话结论

上一轮四根杠杆这轮实际动了**一根半**，而真正的地基已经被并行会话挖出来了：**真实基线是 0 解出，不是 17%，更不是 n=1 的 100%**；瓶颈不是「能力」，是「假阳性口径污染 + 超时太短 + 预算级联饿死」。本计划把顺序钉死：**先止血（修假阳性）、再补血（修超时和预算）、最后测出真数**。第一名不是功能最多的队，是第 10 分钟有真分进账、且知道自己真几分的队。
