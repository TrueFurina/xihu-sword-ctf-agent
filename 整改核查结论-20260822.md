# 西湖论剑 CTF-Agent · 赛前整改核查结论（2026-08-22 20:13）

## 核查方式
**亲自实证**，不引用二手汇报：git 状态、import 验证、全库恒真 grep、pytest 回归、白名单/沙盒/墙钟/看板逐项 `grep` 核对。

## 结论：此前锐评列出的 P0/P1 得分链路问题，已全部闭环

| 问题 | 状态 | 实证证据 |
|------|------|----------|
| P0-1 竞速/平台层恒真（`or True` / `lambda f:True`） | ✅ 解决 | `run.py` `validate_locally` 开关 + 两票互证；全库 `or True`/`lambda f:True` **代码级清零**（仅注释/docstring 提及） |
| P0-2 `Optional` 显式导入 | ✅ 在 | `run.py:13` `from typing import Optional` |
| P0-3 真实 LLM smoke + 端到端 | ✅ 在 | `tests/test_real_llm_smoke.py` 默认 skip、带 key 可跑真实往返 |
| P1-1 `_auto_advisor` 去硬编码 | ✅ 在 | 走统一 `llm.client.ai_chat`，无裸 httpx 端点 |
| P1-2 墙钟 600→480 | ✅ 在 | `config.py:149 hard_wallclock=480`；`core/main_agent.py` HARD 分支 480 |
| P1-3/P1-4 散落文件+一次性脚本归档 | ✅ 在 | 根目录已无 `java_ser.py`/`quick_*.txt`；`scripts/_archive_legacy/` 存在 |
| 误回滚丢失的 fail-closed 白名单 | ✅ 已补回 | `llm/client.py` 注释明确「补回被误回滚的安全修复」 |
| 误回滚丢失的 fail-closed 沙盒 | ✅ 已补回（更细） | `subprocess_executor.py` 参数级白名单 |
| 诊断脚本 3 处 `or True` 死断言 | ✅ 已清零 | 本次修正 `scripts/_smoke_test.py`，构造真实 step 触发分类规则 |

## 未做 / 保留项（均为有意为之，非遗漏）
- **看板 `/api/status`**：未补回。当初即判定为半成品会误报 mock 模式，不补回反而更稳。
- **`run.py` 651 行拆分（P1-5 高风险重构）**：赛前保持不动，维持稳定态。这是正确决策——重构不产生得分、风险高。

## 关于「开赛撞到 429」
用户喊「开赛」时出现的 429 是 **AI 自身推理额度耗尽**，与 CTF 项目代码无关，并非项目未修复的 bug。现已恢复。

## 当前验证基线
- git HEAD：`fc12129`（8/22 治理改造后，协同登记 hook + 看门狗）
- 工作树：除本次 `_smoke_test.py` 修复外干净
- `pytest tests/`：**208 passed, 3 skipped**
- `import run`：OK，`build_race_solver` / `build_solver` / `build_platform_solver` 三函数齐全
- 全库 `or True` 恒真：清零
