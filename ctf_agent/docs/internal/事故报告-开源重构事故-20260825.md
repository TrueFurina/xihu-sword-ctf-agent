# 开源重构事故报告（2026-08-25）

> 范围：第九轮作战计划 1.5。半页。引用 commit 均为本会话实测 HEAD。

## 时间线
- **2026-08-24 22:20** — commit `33b5977`「移除 ctf_agent 内部治理文件」将主仓库回退为"展示版"：单分支单 worktree；8/23–8/24 实质工作（`data/questions_real` 入库、ledger 诚实化、benchmark 修复）经 `git cat-file` 确认**不在对象库**，散落 7 个兄弟目录（wt-final/wt-delivery/ctf_agent_broken 等）。
- 同期"开源整理"动作：① `tools/`·`tests/`·`ctfplatform/`·`scheduler/` 各被多嵌套一层；② `.gitignore` 第107行 `/data/` 锚定整目录，连带屏蔽真题集真值 `data/questions_real/`；③ 测量资产被移出主仓库工作树（git 历史从未跟踪，删即丢，无法 `git show` 捞回）。
- **2026-08-25** — 第九轮核查证实主仓库=展示版（`data/` 文件数=0、pytest 0 可执行、钩子失效），启动恢复。

## 损失清单
1. **诚实水位声明.md**（7902B）：作战真值台账被移出主仓库，外部无法复核真实水位。
2. **KPI_BASELINE.json**（195B）：合并闸基线丢失 → 闸门 fail-open（见根因②）。
3. **测试瘫痪**：`tests/tests/` 嵌套致 pytest collection 即报 import 错误，从"271 passed"跌到 **0 可执行**。
4. **根目录谎言 commit**：`33b5977` 以"移除治理文件"为名，把仓库伪装成干净展示版，实则静默丢弃全部实质工作，制造"健康闭环"假象（8/24 曾据此误报 271 passed）。

## 根因
- **发布与作战同树**：为"开源整理"把发布态与作战态混在同一仓库，清理动作误伤作战资产。
- **闸门 fail-open**：`_merge_gate.py` 基线文件缺失时 `save_baseline(n)` 用当前计数重建并放行——删 `KPI_BASELINE.json` 即可无成本重置棘轮，使造假可悄然过关。

## 修复（本方案）
- **1.2 fail-closed**：`_merge_gate.py` kpi_check 基线缺失即 `sys.exit(1)`（commit `32a3b99`）。删基线不再能重置棘轮。
- **1.3 测试复活**：撤销三层嵌套（commit `32a3b99`+`35114c4`+`27bab8b`），miniconda pytest **285 collected / 0 errors**，实跑 **268 passed / 1 failed（依赖已退役 `data/questions` 混合集，非代码缺陷）/ 14 skipped**。
- **钩子激活**：pre-commit `ROOT` 适配 `ctf_agent` 子目录 + commit-msg message 文件路径修复（commit `c7de4b1`），`git config core.hooksPath ctf_agent/git_hooks` 激活，六门禁 + fail-closed 合并闸随 commit/merge 机器兜底。
- 附带：`.gitignore` `/data/`→`/data/*` 使真题集真值可追踪；`tools/` 去嵌套修复 import（commit `210c798`）。

## 现状水位（诚实标注）
- 真题集 KPI 在 wt-final（8/24 真实环境）实跑确定性管线 = **14/15** 真值匹配（外部 ground-truth，已 sha256 脱敏，零明文）；主仓库 = **13/15**（specialcurve2 缺 8/24 factordb+PARI 补丁，属全量恢复范畴）。
- **LLM 真推理贡献 = 0**（13/15 全由 presolve 静态分析直出）。报"86.7%"须注明含 presolve 直出、LLM 真推理贡献 0。
