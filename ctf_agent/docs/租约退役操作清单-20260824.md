# 租约退役操作清单（P0-3 · 2026-08-24）

> 背景：阶段5 物理层落地后，worktree 车道（一人一 worktree）已从物理层隔离写冲突。
> 租约（`scripts/_lease.py`）从「唯一执法锁」降级为「车道边界提示」——只保留
> pre-commit ③ 兼容接口（scope 提示 + 身份映射审计）与历史回归。
> 本文档是退役的**操作清单**（可执行、可验证），不新增功能。

## 一、文档标注退役（已完成）

| 文件 | 改动 | 状态 |
|---|---|---|
| `scripts/_lease.py` docstring | 顶部加「⚠️ 退役状态」段：降级为车道边界提示、不再承担唯一执法锁、不再新增功能 | ✅ |
| `scripts/_lease.py` docstring | 冻结 `lease_version` 自增声明（实现恒为 0，与 docstring 声称不符——如实标注，消除文档-实现漂移） | ✅ |
| `tests/test_lease.py` docstring | 加「退役状态：历史回归锚点，不新增用例、不删除」 | ✅ |
| `tests/test_lease_multiwriter.py` docstring | 同上 | ✅ |
| `AGENTS.md` 机器配套表 | 「车道隔离」行已注明「租约降级为边界提示」（阶段5 落地时已写） | ✅ |

## 二、精简测试（保留兼容，不删用例）

**决策**：不删除任何租约测试用例（20 个：test_lease 6 + test_lease_multiwriter 14）。
理由：删测试 = 隐藏问题，违背「FIX, DON'T HIDE」；这些用例是 `_lease.py` 兼容接口
（acquire/precommit/status/release/heartbeat）的回归锚点，pre-commit ③ 仍依赖它们
不被破坏。**精简 = 不再新增用例 + 标注退役**，而非删除。

验证命令：
```bash
.venv/Scripts/python.exe -m pytest tests/test_lease.py tests/test_lease_multiwriter.py -q --no-header
# 预期：20 passed（历史回归全绿，接口未破坏）
```

## 三、保留兼容接口（命令面不变，向后兼容）

| 接口 | 调用方 | 保留原因 |
|---|---|---|
| `_lease.py acquire --session --scope` | 会话/协调者手动 | pre-commit ③ 依赖 scope 记录 |
| `_lease.py precommit --session` | `git_hooks/pre-commit` ③ | **必须保留**——门禁依赖 |
| `_lease.py status` | `_session_boot.py` 快速自检 | 车道内租约状态查看 |
| `_lease.py release` | 会话完工 | 释放协调状态 |
| `_lease.py heartbeat` | 手动续租 | TTL 兜底 |

**禁止**：修改这些接口的签名/语义（向后兼容是硬约束）。

## 四、停止新功能（冻结清单）

以下功能**明确冻结，不再实现**（避免文档-实现漂移复发的温床）：
- ~~lease_version 接管时自增~~（docstring 已如实标注，实现恒为 0）
- ~~fencing 签名~~（威胁模型是「会犯错的队友」非恶意攻击者）
- 任何新的租约语义（多写者并发、接管策略增强等）——物理隔离已由 worktree 提供

## 五、回归验证（提交前必跑）

```bash
bash -n git_hooks/pre-commit && echo "pre-commit 语法OK"
.venv/Scripts/python.exe -m py_compile scripts/_lease.py && echo "_lease.py 语法OK"
.venv/Scripts/python.exe -m pytest tests/test_lease.py tests/test_lease_multiwriter.py -q --no-header  # 20 passed
```

## 六、退役边界（什么情况下需要撤销退役）

仅当**重新出现「单工作树多会话直接写」**（worktree 车道被绕过）时，租约才需恢复执法语义；
当前 worktree 车道 + main 直提拦截 + 合并闸门已从物理层封死该路径，退役是安全的。

## 七、影响评估

- **无影响**：pre-commit ③（租约门禁）照常运行；`_session_boot.py` 自检照常；合并闸门照常。
- **收益**：停止为租约开发新功能（节省维护成本）；消除 lease_version 文档-实现漂移；
  测试语义从「核心执法」明确为「历史回归」，新会话不再误以为租约是主锁。
