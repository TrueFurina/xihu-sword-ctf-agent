### 10739 REVERSE-03 NativeAOT（未解出——工具链受限止损）

- **尝试轮数**：9 轮（远超 R3 止损线 3 轮）
- **已确认**：程序 = .NET 8 NativeAOT（.managed+hydrated 段实锤）；格式校验 `DASCTF{...}`（无长度校验，8-40 字符均过）；IsFlag 主逻辑函数（PDB 符号 `NAOT_Program____Main___g__IsFlag_0_0`）；FlagBit 枚举 0-63（疑似位运算）
- **卡点**：① NativeAOT 字符串加密（PDB 符号 `__Str_<明文>__<sha256>`，EXE 内无明文 flag 常量）② 无 IDA/Ghidra/PDB 解析工具（pdbparse/dashes 安装失败，gdb/objdump/pefile 不可用）③ IsFlag 无法定位到函数体（无符号 RVA）
- **止损结论**：需 IDA/Ghidra + ghidra-nativeaot 插件或完整 PDB 解析工具链才能继续；本地预算内不可解，**不计入解题数**

### 10736 PWN-01 what_heap（未解出——环境性止损：flag 载体不存在）

- **尝试轮数**：3 轮（附件分析 + R1 writeup + 环境确认）
- **已确认**：what_heap = 64-bit x86-64 ELF（EXEC 无 PIE、canary 开、.got.plt）；经典堆菜单（1add/2edit/3delete/4show + size/index）；libc 最高 GLIBC 2.27（**无 safe-linking**，tcache poisoning 理论上可用，比 SoSafeMinePool 的 2.32 简单）；仓库 4 个 pwn skill 可复用
- **止损根因（环境性，非能力性）**：
  1. 题面 flag 字段为空 + what_heap/libc 二进制内无静态 flag 明文 → **flag 是服务端动态 flag，本地不存在可解的 flag 载体**
  2. 题面 endpoints 为空（比赛已结束，无远程靶机可连）
  3. 本机无 Linux 运行环境（WSL 无已安装分发、Docker engine 无法连接）→ 即使构造出 exploit 也无法本地验证
  4. pwntools 未安装
- **止损结论**：攻击链理论上可行（tcache poisoning + 泄露 libc），但**无 flag 可拿、无靶机可连、无环境可验证**——环境性不可解，**不计入解题数**。若后续有靶机/环境可复用攻击链。
