# 环境变量 & 模型矩阵可用性确认（2026-08-21 18:30）

> 目的：逐一确认所有写入系统环境变量的 API Key 是否真实可用，并把 `ultra` 竞速矩阵
> 修正为「实测存活」源，避免比赛中对失效源空转 402/403。

---

## 一、注册表落盘审计（HKCU\Environment）

全部 **29 个变量均已正确固化**（新进程真值源）：
- API Key（白名单内）：TOKENHUB / ARK / ZHIPU / ZHIPU_API_KEY_2 / MOONSHOT / SILICONFLOW / SENSENOVA / DEEPSEEK / DASHSCOPE / QIANFAN / MIMO / XFYUN（+3 把讯飞备份 key）
- API Key（off-whitelist，仅存不路由）：GROK / TOKENROUTER(+BASE_URL) / OPENROUTER / PEEZY / MODELSCOPE
- 运行时：USE_REAL_LLM=1 / LLM_PROVIDER=baidu / ENFORCE_WHITELIST=1 / **RACE_PROFILE=ultra** / RACE_WALLCLOCK=300 / MAX_CONCURRENCY=4；LIGHT/HEAVY 已置空（避免 deepseek-chat 打千帆端点 404）

---

## 二、真实调用探测结果（每个 Key 打一次最小 chat completion）

### ✅ 白名单内——可用（HTTP 200，比赛中可路由）

| 源 | 模型 | 状态 | 备注 |
|---|---|---|---|
| TokenHub | hy3 | ✅ 200 | 腾讯免费包主模型 |
| TokenHub | deepseek-v4-flash | ✅ 200 | 腾讯免费包（深V4-Pro 额度已耗尽） |
| Ark（豆包） | doubao-seed-2-1-pro-260628 | ✅ 200 | 200 万 token/日 自动刷新 |
| 智谱（优先级 key） | glm-4.7 | ✅ 200 | `ZHIPU_API_KEY`（23 号到期，狠狠榨干） |
| 智谱（优先级 key） | glm-5.3 | ✅ 200 | 同上 |
| 智谱（优先级 key） | glm-5.2 | ✅ 200 | 同上 |
| Moonshot（Kimi） | kimi-k2.6 | ✅ 200 | |
| 讯飞 | lite | ✅ 200 | 永久免费无限额度（主用） |
| 千帆（Baidu） | ernie-3.5-8k-preview | ✅ 200 | 全系统最强单源 |
| 小米 MiMo | mimo-v2.5-pro | ✅ 200 | |
| 百炼（Qwen） | **qwen-plus** | ✅ 200 | ⚠️ qwen3.7/3.8 免费额度已耗尽(403)，改用 qwen-plus |

### ❌ 白名单内——失效/耗尽（已从 ultra 剔除，否则空转）

| 源 | 错误 | 原因 / 处理 |
|---|---|---|
| DeepSeek 官方 | 402 Insufficient Balance | 账户余额耗尽，需充值 |
| SiliconFlow（硅基） | 402 余额不足 | 整个账户余额 0，需充值 |
| 百炼 qwen3.7-plus / qwen3.8-max | 403 免费额度耗尽 | 改走 `qwen-plus`（可用） |
| TokenHub deepseek-v4-pro | 402 免费额度耗尽 | 免费包该模型额度用尽 |
| TokenHub kimi-k3 | 持续 NET_TIMEOUT | 网关路由超时（非 key 失效）；赛中由熔断器剔除，不影响其它源 |
| SenseNova（商汤） | 403 Forbidden(code 7) | ⚠️ **需你到商汤控制台确认**：key 是否开通 API 访问、模型名是否需带版本、资源组是否正确（控制台显示额度 100% 剩余，但 API 拒） |

### 🔌 off-whitelist——Key 已存，不进比赛路由（避免被白名单拦截/取消资格）

| 源 | 探测结论 | 说明 |
|---|---|---|
| Grok（xAI） | 超时 | 国内需梯子，预期内；key 未验证 |
| TokenRouter | 503 model_not_found | key 鉴权通过（返回 request id），但默认 distributor 组未开通模型通道 → 需在控制台选模型 |
| OpenRouter | 404 模型名过时 | 试的 `google/gemini-*` 均已下线；key 未验证，需更新模型名 |
| Peezy | DNS 解析失败 | 端点 URL 不确定（试了 api.peezy.ai / api.p0systems.com 均不通），非 key 问题 |
| ModelScope（魔搭） | DNS 解析失败 | 端点 URL 不确定（试了 api.modelscope.cn / modelscope.cn 均不通），非 key 问题 |

> off-whitelist 三个（Peezy/ModelScope/TokenRouter）是**端点或模型名不匹配**，不是 key 失效；待确认正确端点后可手动调用，但绝不进 `ctf_agent` 比赛路由。

---

## 三、修正后的 ultra 竞速矩阵（11 路实测存活）

```python
"ultra": dict(
    models=("qwen-plus",),                        # 百炼（qwen-plus 实测 200）
    providers=("baidu", "glm", "xfyun"),         # 千帆ernie + 智谱优先级key + 讯飞lite无限
    tokenhub_models=("hy3", "deepseek-v4-flash"),  # 腾讯免费包
    extra_models=(("ark", "doubao-seed-2-1-pro-260628"),  # 豆包
                  ("moonshot", "kimi-k2.6"),                # Kimi
                  ("glm", "glm-5.3"),                        # 智谱优先级key
                  ("glm", "glm-5.2"),                        # 智谱优先级key
                  ("mimo", "mimo-v2.5-pro")),                # 小米MiMo
)
```

逐路 `resolve_api_key` 全部一一对应读到正确 key ✅。

---

## 四、待你处理（手动项）

1. **商汤 SenseNova**：到控制台确认 key 有效性 / 模型全名 / 资源组（当前 API 403）。
2. **DeepSeek / 硅基流动**：余额耗尽，如需使用请充值。
3. **TokenHub kimi-k3**：网关偶发超时，赛中由熔断器自动剔除，无需处理（若稳定失败再观察）。
4. off-whitelist（Grok/TokenRouter/OpenRouter/Peezy/ModelScope）：如需用，自行确认端点与模型名，且**只用于非比赛场景**。

---

## 五、比赛启动铁律（重申）

```bash
cd E:\Program\西湖论剑\ctf_agent
bash setup.sh                                  # 一键就绪 + 固化全部 key/变量
.venv/Scripts/python.exe scripts/_race_start.py --compete   # 正式赛作战
```
**永远用 `bash setup.sh` 或全新终端启动**，别复用带陈旧 `CTF_AGENT_*` 的老 shell（陈旧值会遮掉注册表）。
限流严重时赛中降级：`set CTF_AGENT_RACE_PROFILE=live`（3 路）或 `full`（6 路）。
