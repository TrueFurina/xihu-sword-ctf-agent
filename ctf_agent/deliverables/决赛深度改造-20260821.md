# 决赛深度改造报告（2026-08-21 18:45）

> **场景**：@scene 决赛备战（W1 止血与验证第 1 天）| 依据：产品管理总纲 SSOT（B-01~B-33）
> **基线**：pytest 120 passed → **136 passed**（+13 新测试 +3 并行会话测试）
> **验证**：白名单预检全绿 / import 冒烟 OK / LLM 存活矩阵确认

---

## 一、本轮代码改造（6 个文件 + 2 个新测试）

| Backlog | 改动 | 文件 | 验收证据 |
|---|---|---|---|
| **B-20** | 重型模型触发修正：`_should_upgrade_heavy()` 收敛判定——HARD/VERY_HARD 首步重型；**crypto/pwn/reverse 的 EASY/MEDIUM 仅卡壳(attempt≥1)才重型**，简单题走轻量不烧 reasoner | `core/main_agent.py` | 新增 `tests/test_final_prep_heavy.py` 9 例全绿（EASY crypto 首步 False / HARD crypto 首步 True） |
| **B-23** | firstblood 附件雷修复：watch 模式与 `--solve` 路径弃用 `_to_question`（attachments=[]），改复用 `build_platform_solver`（get_challenge 补全 + 附件下载 + 靶机注入） | `scripts/_scan_firstblood.py` | 与 `_race_start.py --firstblood` 已修路径对齐；import 冒烟 OK |
| **B-18** | 看板任务状态持久化（`data/results/dashboard_tasks.json`，临时文件原子替换）+ `_run_all` 运行锁（并发 POST 不重复解题双烧额度） | `web/server.py` | 新增 `tests/test_web_dashboard_persistence.py` 4 例全绿（roundtrip/损坏容错/并发锁/串行） |
| **B-04** | e2e 平台验证脚本增强版：`scripts/_e2e_verify.py`（list→get_challenge→题面非空→附件 HEAD/下载→靶机解析；`--all`/`--deep` 模式；退出码 0/1/2） | `scripts/_e2e_verify.py` | 实测连平台返回 0 题（赛后平台关闭）正确 WARN；setup.sh `--e2e` 已由并行会话接入 |
| — | 删除重复分类脚本（并行会话 `_category_regression.py` 更完整，用附件名 ground truth） | `scripts/analysis/_verify_challenge_class.py` | — |

## 二、复核结论：SSOT 标记"未修"但实际已修的项

侦察确认以下 Backlog 项**当前代码已实现**（SSOT 状态过时，建议总纲更新）：

- **B-05 提交链路 fail-open**：`poller._submit_with_retry` L518-530——`request_failed` 不计入 consec_fail，仅平台返回"flag 错误"才计（≥30 或连续 5 才熔断）✅
- **B-07 429 风暴 poller 退避**：失败题进重试队列（2-5min 延迟 ×3 次），fast_interval 钳制，列表失败间隔倍增封顶 300s ✅
- **B-15 工具调用率下限**：`phases.extract_flag` 无工具证据即拒、act_step/plan_step 有附件未分析强制 file_analyze、连续 3 步 reason 注入强制转工具提示 ✅
- **B-16 token 记账并发**：run.py 已改 ContextVar `_usage_cv` 累计，`_LAST_USAGE` 模块全局仅向后兼容 ✅
- **B-22 死代码**：`scheduler/rate_limiter.py`/`model_router.py` 已不存在（scheduler/ 仅剩 budget/task_pool 活代码）✅
- **B-24 平台层**：submit_flag isdigit 防护（L514）、AsyncClient 按事件循环复用（P1-12）、get_access 公开方法 ✅
- **B-21 快速失败**：已实现为数据完整性检查（附件/靶机/题面全缺 + 2 步 reason 才止损）✅
- **B-08 分类回归**：并行会话 `_category_regression.py` 输出 32 题全列表（附件名 ground truth，web 23 题），75% misc 误判已纠正 ✅
- **B-12 确定性 skill**：并行会话已沉淀 4 个（crypto_high_exponent / crypto_pkcs1_padding_oracle / misc_bigfile_analysis 等）✅

## 三、LLM 存活矩阵（决赛可路由，11 路实测 200）

`TokenHub hy3 / deepseek-v4-flash` · 豆包 · **智谱 glm-4.7/5.3/5.2（优先级 key，23 号到期狠榨）** · Kimi · 讯飞 lite · 千帆 ernie · MiMo · 百炼 qwen-plus

已剔除（否则空转）：DeepSeek 官方 402 / 硅基 402 / qwen3.7/3.8 403 / TokenHub dsv4-pro 402 / kimi-k3 网关超时 / **SenseNova 403 待控制台确认**。

## 四、协作说明（多会话并行，SSOT 铁律第 4 条）

- 检测到并行会话活跃：`3449e16`（skills 沉淀+归档 55 项）、`2009aa6`（e2e 接入 setup.sh + 分类回归 + ultra 落库）、`3c9e6a4`（llm/client usage 解包）。
- 冲突规避：本轮不提交 git（index.lock 被占用），**6 个文件改动已就绪待提交**；未触碰 llm/client.py / setup.sh / scripts/analysis。
- 建议：并行会话结束后，由任一会话统一 `git add -A && git commit` 本轮改动。

## 五、待办与风险（交接收口）

1. ⏳ **git 提交**：本轮 6 文件 + 2 测试（index.lock 释放后立即提交）
2. 🔴 **SenseNova 403**：用户到商汤控制台确认 key 开通/模型全名/资源组（唯一需人工的白名单项）
3. 🔴 **B-33 晋级核实**：0 解出是否仍有决赛资格（用户决策，前置项）
4. 🟡 **e2e 实跑**：决赛前 1h `bash setup.sh --e2e`（平台开放时，当前赛后返回 0 题属正常）
5. 🟡 **B-06 补强**：已有 test_wallclock_cancel（子进程杀/kill），竞速外层取消可加集成测试
6. 🟡 **B-02 git 清理收尾**：与并行会话确认后统一提交归档与附件删除

## 六、决赛启动铁律（重申）

```bash
cd E:\Program\西湖论剑\ctf_agent
bash setup.sh                              # 一键就绪（含 --e2e 平台验证）
.venv/Scripts/python.exe scripts/_race_start.py --probe    # 探测
.venv/Scripts/python.exe scripts/_race_start.py --compete  # 作战（ultra 11 路存活矩阵）
```
**永远用 setup.sh 或全新终端启动**；改码必重启；e2e 是唯一判据；决赛前 48h 只读冻结。
