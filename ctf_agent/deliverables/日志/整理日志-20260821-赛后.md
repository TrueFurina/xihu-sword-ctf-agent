# 整理日志-20260821-赛后（文档处理行动 · 第二次）

> 场景：@scene#4 文档处理 — 赛后项目文档梳理
> 执行时间：2026-08-21 17:53-17:58（正式赛 14:00-17:00 结束后）
> 原则：延续上午整理约定——**只移动/归档，不删除任何有 git 历史的文件**；运行路径（run.py / config.py / skills / data 内部）一律不动。

## 一、扫描发现的问题（17:53-17:55）

正式赛期间在 `ctf_agent/` 顶层又产生了大量散落文件，共 31 项：

1. **赛后一次性脚本 11 个**：solve_all.py / solve_det.py / solve_local.py（赛后解题）、test_fetch.py / test_providers.py（测试）、probe_state.py / _examine.py / _quick_solve.py / _check_syntax.py（辅助调试）、fetch_challenges.py / get_misc01.py（抓题）。
2. **临时解压目录 17 个**：tempdir、tempdir2、tempdir_extract、tempdir_extract2、tempdir_misc04、tempdir_pwn02、tempdir_pwn02_extracted、tempdir_rev04、tempdir_web04、tmp_crypto04、tmp_misc03、tmp_misc04、tmp_misc04_extract、tmp_web04、extract_misc04、extract_rev05、pwn02_extract（赛时附件解压产物，多为嵌套空 tempdir）。
3. **赛题附件 4 项**：idol.zip / inner.zip / src-file.zip + `data/task.py`（CRYPTO 题源码，RSA 题 task，误放 data 根下）。
4. **运行日志**：race.log（395KB，赛时主日志，gitignore 已忽略）。
5. **备份文件**：run.py.bak_20260821（赛时 run.py 备份，有 git 历史）。
6. **根目录新增平台文档 2 个**：api_doc.md（AI Agent API）、gateway_doc.md（大模型网关接入）。

## 二、安全性验证（17:55）

- 11 个散落脚本 grep 全代码库 **0 处 import 引用** → 可安全归档为一次性脚本
- `data/task.py` grep 全代码库 **0 处引用** → 确认为赛题附件源码，改名归位
- `scripts/_race_start.py` / `config.py` / `setup.sh` / `start_race.bat` / `start_web.bat` 的赛时修改 **保留不动**（正式代码）
- `scripts/analysis/_postrace_*.py` 已位于正确位置（赛后分析流水线），不动
- `logs/` 下赛时日志（compete_*.log / race_*.log / worker_*.log 等）已是合理落点，不动
- `data/` 内部运行数据（race_extract / race_tmp / reports / results / handwork）属数据区，不动

## 三、执行记录（17:55-17:58）

| # | 操作 | 结果 |
|---|------|------|
| 1 | 11 个一次性脚本 → `solutions/legacy_tools/` | 11/11 ✓ |
| 2 | 17 个临时解压目录 → `logs/debug_artifacts/` | 17/17 ✓ |
| 3 | idol.zip / inner.zip / src-file.zip → `data/race_attachments/` | 3/3 ✓ |
| 4 | data/task.py → `data/race_attachments/task_CRYPTO题源码.py`（防误识别为代码库文件） | ✓ |
| 5 | race.log → `logs/race_20260821.log` | ✓ |
| 6 | run.py.bak_20260821 → `logs/debug_artifacts/`（git mv，保留历史） | ✓ |
| 7 | 根目录 api_doc.md / gateway_doc.md → `docs_archive_参考资料/` | 2/2 ✓ |
| 8 | 更新 `_INDEX.md`（赛后状态 + 目录结构 + 速查表） | ✓ |
| 9 | 本日志 | ✓ |

## 四、git 落库（17:58-18:02）

- 有 git 历史的文件（run.py.bak_20260821、_examine.py 等 5 个被跟踪脚本）经 `git mv`/`git add -A` 识别为 rename，历史保留，可回退
- commit 1：`docs: 赛后整理-归位 ctf_agent 顶层散落文件与根目录平台文档`
- **仓库瘦身（追加发现）**：`git add -A` 时发现 `data/race_extract/`（2.6G，赛题附件解压产物，含 pycryptodome 源码等）此前已大量入库，`.git` 膨胀至 3.7G
  - 补充 .gitignore：`data/race_extract/`、`data/handwork/`（439M）、`data/race_tmp/`、`data/temp_crypto03/`
  - `git rm --cached` 解除已跟踪文件（本地文件全部保留）
  - commit 2：`chore: 赛时运行数据移出 git 跟踪`
- commit 3：`chore: 归位赛时调试输出 _tmp_test_out.txt`
- ⚠️ **遗留待决策**：.git 历史对象（3.7G）不会因 --cached 变小，如需彻底瘦身须 `git filter-repo` 重写历史（会破坏含安全修复 commit 的历史，建议先 push 备份再操作，或放弃瘦身）

## 五、整理后结构

- **ctf_agent 顶层仅剩**：启动/入口文件（run.py、setup.sh、start_*.bat、config.py、pyproject.toml、.gitignore）+ 正式模块目录（agents/core/ctfplatform/skills/tools/...）
- **根目录仅剩**：核心文档（todo.md、赛前手雷手册、赛题问答汇总）+ 4 个归档/交付目录 + _misc + ctf_agent
- **日志统一落点**：`ctf_agent/logs/`（运行日志）+ `.workbuddy/memory/`（工作区日志）

## 六、风险与说明

- 临时解压目录中若有残留证据（如赛题附件原文），已随目录整体保留在 `logs/debug_artifacts/`，未删除
- `logs/debug_artifacts/` 体积可能较大（含 17 个解压目录），如需释放空间，确认后另行清理
- 本次整理未删除任何内容；根目录大文件「初赛问题汇总及解答」（642KB）为官方问答汇总，保留
