# 西湖论剑 CTF-Agent · 新会话对接指南（2026-08-23）

> 写给**下一个接手会话**（人类或 AI）的启动文档。开工前**必读**，先读 `ctf_agent/AGENTS.md`（治理铁律）与本指南，再动任何文件。

---

## 〇、三句话现状

1. **git 已推倒重建并双仓落地**：代码仓 `TrueFurina/xihu-sword-ctf-agent`（19+ commit，PRIVATE）+ 文档仓 `TrueFurina/xihu-ctf-deliverables`（PRIVATE，8/23 新建）——**GitHub 现在是唯一真相源**，本地 `.git` 历史上坏过一次，别再依赖它。
2. **明文密钥已止血**：全项目 5+1 个文档里的 `ak_live_*` 明文已替换为 `${XIHU_PLATFORM_AK}` / `${XIHU_PLATFORM_AK_2}` 引用，值已 `setx` 持久化到 **Windows 用户级环境变量**（HKCU），全量复扫 0 命中。
3. **磁盘是头号风险**：E: **97% 满**（8.8G 空闲）、C: **93% 满**（24G 空闲）——**损坏根因就是盘满**（3.3G 旧git备份 + 2.7G bundle + 13G data 占着），任何大 git 操作都可能再炸一次。

---

## 一、当前状态（已验证事实）

| 项 | 状态 | 证据 |
|---|---|---|
| 代码 git | main=`1c3dc15`，远端哈希一致，fsck=0，19+20 个 commit 全推 GitHub | `git -c http.proxy= -c https.proxy= ls-remote origin main` |
| 文档仓库 | `xihu-ctf-deliverables`，PRIVATE，main=`28e2943`，上传前密钥扫描 0 命中 | `gh repo view xihu-ctf-deliverables` |
| 密钥环境变量 | `XIHU_PLATFORM_AK`(len12) / `XIHU_PLATFORM_AK_2`(len40) 已 setx | `[Environment]::GetEnvironmentVariables('User')` |
| 损坏 .git | `_misc/git_broken_20260822_1103` = 9 文件重建骨架，非原始库；原始旧历史在 2.7G bundle | `git bundle verify` = is okay |
| 旧 git 备份 🆕 | `_git_corrupt_backup_20260822/` = 3.3G 完整旧 .git 目录（HEAD=fix/sharpcritique-attack-20260822，**含泄露密钥 commit 1715a15**）——第 3 份泄露源，勿上传 | `git --git-dir=_git_corrupt_backup_20260822 cat-file -e 1715a15` = YES |
| 明文密钥 | 全部文档 0 命中（6 文件已脱敏） | `grep -rnoE 'ak_live_|sk-|ghp_' deliverables .workbuddy` |

## 二、未完成项（按优先级）

1. **【用户动作】平台侧轮换密钥**：`ak_live_*` 已在旧 git 历史/bundle/旧 .git 备份泄露过，脱敏不等于作废。**轮换前三份泄露源（bundle + _git_corrupt_backup + 损坏现场）都是雷**。用户已选择"暂不轮换"——轮换后应：更新 `XIHU_PLATFORM_AK*` 环境变量 → 删除 `_misc/_backup_pre_cleanup.bundle`（2.7G，含明文）→ 删除 `_git_corrupt_backup_20260822/`（3.3G，含 1715a15 泄露 commit）→ 删除 `_misc/git_broken_*`。
2. **【高风险】磁盘告急**：E: 8.8G / C: 24G 空闲。可清理候选（均需用户确认）：`data/race_attachments`(3.5G)、`data/race_extract`(2.7G)、`data/platform_downloads`(2.6G)、`data/work_web`(1.8G)、`data/results_backup_20260821_1427`(914M)、`data/handwork`(452M)——若赛题已解完且不需要留档，可腾出 ~12G；`_misc`/`_git_corrupt_backup` 的 6G 要等密钥轮换后删；**禁止在本机再写大文件**。
3. **【代码侧】CTDE 双轨对账**：根目录 `多智能体协同协议-20260822.md`（规范层）+ `scripts/_lease.py`/pre-commit（执法层）已并入 AGENTS.md；下次并行会话动代码前必须 `export CT_AGENT_SESSION=xxx` + `acquire` 租约。
4. **【可选】把 `deliverables/` 里的复盘文档接入文档仓库**：`_upload_stage/` 是文档仓的本地工作区（已 push），后续新文档改这里再 push。
5. **【临时文件】侦察脚本清理**：`E:\Program\西湖论剑\_*.py/_*.ps1/_*.out/_*.txt/_*.diff` 为本会话临时产物（含掩码数据），无保留价值，可删。

## 三、验证命令（接手后先跑）

```bash
cd /e/Program/西湖论剑/ctf_agent
git status --short                       # 应干净（或在飞有说明）
git fsck --no-reflogs                    # 应 0 error
git -c http.proxy= -c https.proxy= ls-remote origin main   # 远端=本地
.venv/Scripts/python.exe -m pytest tests/ -q -m "not slow" # 全绿
.venv/Scripts/python.exe scripts/_lease.py status          # 租约状态
# 密钥残留检查（应全 0）：
grep -rnoE 'ak_live_[A-Za-z0-9]{4,}|sk-[A-Za-z0-9]{12,}|gh[pso]_[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}' deliverables .workbuddy
df -h /e /c                                # 磁盘余量（红线 <5G）
```

## 四、安全红线（违反 = 事故重演）

1. **密钥只进环境变量**：任何文件禁止明文 key；`setx` 只写用户级（HKCU），值不打印不落盘。
2. **bundle / 旧 .git 备份不上传、不删除**（除非密钥已轮换）：`_misc/_backup_pre_cleanup.bundle`(2.7G) 是旧 229-commit 历史的唯一备份；`_git_corrupt_backup_20260822/`(3.3G) 是含 1715a15 的旧 .git 目录——两者都是明文泄露源，也都在 E: 盘上占 6G。
3. **以 GitHub 为真相源**：本地 `.git` 坏过（盘满），重建本地库用 `git clone https://github.com/TrueFurina/xihu-sword-ctf-agent.git`。
4. **改码必走门禁**：pre-commit **三门禁**（密钥扫描 `_scan_secrets.py` + 假水位扫描 `_honesty_scan.py` + 租约 `_lease.py`）已实装；绕过 = 裸奔。**带红提交 = 事故**（2026-08-23 `1d91f68` 带红提交教训：测试红了还提交，哨兵开火还带着弹孔走）。
5. **网络**：GitHub 直连有波动窗口；git 操作用 `-c http.proxy= -c https.proxy=` 直连；代理 7890 是死的。
6. **不要在 E:/C: 写大文件**（磁盘 98%/99% 满，写满会再次损坏 git 对象）。
7. **CTDE 协议（规范层）与 lease+hook（执法层）冲突时以执法层为准**。
8. **假水位禁令（2026-08-23 强化）**：平台 accepted=0 时，任何"解出数 X→Y 递增 / +N 真实解出 / 真实解出…flag / 将功补过"表述都是**离线刷题冒充战报**——写文档前先问"平台 accepted 了吗"，没有就写"离线/本地推导，非平台 accepted"。违规会被 pre-commit 假水位门禁拦截，且 test_report_honesty.py 会红。`99fb169`（10696"真实解出"）证据链断裂教训：replay_real_run.log 无 10696 记录、日志是本地 LLM 复盘非平台提交——**声称"真实解出"必须能指出平台 accepted 证据，否则就是第四次未遂**。

## 五、提交前门禁清单（每次 commit 前逐项过，缺一不可）

```bash
cd /e/Program/西湖论剑/ctf_agent
export CT_AGENT_SESSION=<你的会话名>                 # ① 身份
python scripts/_lease.py acquire --session $CT_AGENT_SESSION --scope "认领的文件域/**"   # ② 租约（越界会被 hook 拒）
git status --short                                    # ③ 确认只暂存你要提交的（勿夹带他人改动）
.venv/Scripts/python.exe -m pytest tests/ -q -m "not slow"   # ④ 全量门禁必须绿（红 = 不提交！）
git add <文件> && git commit -m "..."                 # ⑤ hook 三门禁自动拦：密钥/假水位/租约
python scripts/_lease.py release --session $CT_AGENT_SESSION   # ⑥ 完工释放租约
```

**红线**：测试红 → 先查是不是别人改了数据（git status 看 D/M）→ 恢复 → 再跑绿 → 才提交。**绝不带红提交**（`1d91f68` 教训）。

## 六、关键位置速查

- 代码仓库：`E:\Program\西湖论剑\ctf_agent`（git 根）
- 文档/复盘：`E:\Program\西湖论剑\deliverables`（= 文档仓工作区 `_upload_stage`）
- 危险物（勿动）：`E:\Program\西湖论剑\_misc`（bundle、损坏 git、旧密钥探针）、`E:\Program\西湖论剑\_git_corrupt_backup_20260822`（3.3G 旧 .git，含 1715a15 泄露 commit）
- 治理铁律：`ctf_agent/AGENTS.md`；CTDE 协议：`E:\Program\西湖论剑\多智能体协同协议-20260822.md`（已并入 deliverables/协同协议/）
- 环境变量：`DASCTF_TOKEN`、`CTF_AGENT_LLM_API_KEY`、`CTF_AGENT_LLM_BASE_URL`、`XIHU_PLATFORM_AK`、`XIHU_PLATFORM_AK_2`、`GITHUB_TOKEN`
