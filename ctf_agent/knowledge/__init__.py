"""知识增强模块（IDEA-5 Writeup RAG 务实落地）。

设计原则（贴合本项目离线可复现 + 诚实约束）：
- 知识库 = 项目**自有真实资产**（已验证解法技术文档 + 55+ skill 文档），
  不依赖外网爬取（BUUCTF/攻防世界等爬取在本环境不可复现、且违背诚实红线）。
- 检索核心 = 零依赖 BM25（离线可测、确定性）。
- 嵌入重排 = 可选懒加载（传入 embed_fn 即启用混合检索；无模型时自动降级 BM25-only）。
- 默认不启用：CTF_AGENT_WRITEUP_RAG=1 才构建并注入，保证基线零回归。
"""
